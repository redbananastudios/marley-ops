node_modules
.next
.git
.env*
npm-debug.log*
Dockerfile
.dockerignore
README.md
docs
supabase/volumes
*.local
.vercel
coverage
# Standalone training-video pipeline (Remotion/Playwright/ElevenLabs) — never
# shipped in the app image. Its own package.json + generated media artifacts.
# Anchored to root so it never excludes public/training (the embedded videos).
/training
