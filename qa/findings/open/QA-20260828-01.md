---
id: QA-20260828-01
status: open            # open | fixing | fixed-pending-verify | reopened | closed
class: risky            # touches the auth/login critical path — Peter's call
severity: high
surface: /login → post-sign-in redirect (app/(auth)/login/page.tsx `router.push("/")`) into app/(dashboard)/page.tsx
found_by_run: 2026-08-28T04:08Z (scheduled audit — health gate)
branch:                 # repair agent fills in
---

## Summary
Signing in on staging with a correct email/password now takes ~26-28 seconds to actually land the user past `/login` — the Supabase auth call itself succeeds in under a second, but the client stays on `/login` showing "Signing in…" for that whole window because `router.push("/")` waits on the dashboard route's server render, which blocks on a chain of external calls. This is severe enough that it failed **every** authenticate-office/estimator/crew step in last night's full CI run, so the entire 178-test e2e suite (175 tests) never ran on the commit currently deployed to staging (`b653e0d`).

## Repro
1. No UI role needed to observe the CI failure — it's in the GitHub Actions history: `redbananastudios/marley-ops` → Actions → run `33121391706` ("CI + Deploy to Staging", commit `b653e0d`, 2026-08-27T22:10Z) → job `e2e` → step "Playwright e2e against the live staging deployment". All 3 `[setup] › e2e/fixtures/auth.setup.ts` tests (`authenticate office`, `authenticate estimator`, `authenticate crew`) failed on a `locator.click`/`locator.fill` 15000ms timeout; 175 downstream tests "did not run".
2. Live reproduction this run (marker user `QA-SENTINEL HealthCheck`, minted via service key, deleted + verified 0 at cleanup): launched Chromium against `https://staging.ops.marleymoves.co.uk/login`, filled email/password, clicked "Sign in".
   - First attempt: `page.waitForURL` (15s budget) timed out still on `/login`.
   - Instrumented attempt: the Supabase `POST .../auth/v1/token?grant_type=password` returns `200` in under 1s and the `sb-*-auth-token` cookie is set immediately, but the page body still reads "Signing in…" 8s later with no console error, no `pageerror`, no failed request — just silence.
   - Same attempt given a 55s budget: succeeded, but only after **27.9s**, landing on `/`.
3. Expected: the redirect off `/login` should be near-instant once the Supabase token call returns (as it always has been — nothing in `app/(auth)/login/page.tsx` or the diff between the last fully-green CI run (`d21c674`, 2026-08-27T20:35Z) and the current deployed commit (`b653e0d`) touches this page or the dashboard route; that diff is Zoho/ledger-only).

## Evidence
- CI job logs (`get_job_logs`, run `33121391706`, job `98689707886`): all three `auth.setup.ts` tests fail on the `/login` step before any of the 175 downstream tests run.
- Live timing capture (this run, throwaway marker user, deleted after):
  - `click` → cookie `sb-nrghwyfakrgobcczuuca-auth-token` present within ~1s.
  - `click` → URL leaves `/login` only at **t≈27.9s**.
- Table-size check (rules out "big tables make the dashboard's unbounded scans slow" as the cause): `leads`=23, `appointments`=5, `quotes`=16, `profiles`=4 rows on staging right now — trivially small, not the bottleneck.
- Code read: `app/(dashboard)/page.tsx` (the page `router.push("/")` lands on for a non-estimator) fully `await`s `syncSanityLeads({ incremental: true })` (8s timeout, `lib/sync/sanity-leads.ts:137`) **before** even starting `fetchExternalPanels()` (PostHog `fetchWebsiteFunnel` ×3 periods at a 4.5s timeout each, run concurrently, plus Google Ads `fetchAdSpend` ×3 periods each behind its own OAuth-token fetch, 5s + 6s timeouts, `lib/google-ads.ts:39,87`) — every leg is individually fail-soft (`.catch(() => null)`), but they are **sequential**, not raced/parallel, with each other. Worst case that's roughly 8s (Sanity) + up to ~11s (Google Ads chain) + network/build overhead before the dashboard RSC payload can return — squarely in the observed ~27s range whenever the external services (Sanity/PostHog/Google Ads) are slow to answer or genuinely timing out, rather than fast.

## Suspected cause
`app/(dashboard)/page.tsx:85-90` — `await syncSanityLeads(...)` blocks the whole render before `fetchExternalPanels()` (PostHog + Google Ads) is even kicked off, so their worst-case latencies stack instead of overlapping, and none of it is bounded by an overall page-level budget. The dashboard's own comment at line 44 already acknowledges these are "the slow leg... don't need per-click freshness" but only that leg is cached (5 min) — Sanity's incremental sync isn't, and isn't parallelised with it either. Every login funnels through this page (`router.push("/")` in `app/(auth)/login/page.tsx:28`), so any slowness here is universal, not feature-scoped.

## Verify
The exact assertion the next audit must re-run to close this: mint a marker user, time a live Playwright login (fill email/password, click "Sign in", `waitForURL` leaving `/login`) against `https://staging.ops.marleymoves.co.uk/login` — must land within Playwright's normal action budget (≤10s) consistently across 3 attempts, AND the `authenticate office`/`estimator`/`crew` `[setup]` steps must pass in the next full (non-docs-skip) CI run on `staging`.
