---
id: QA-20260819-01
status: fixed-pending-verify
class: risky
severity: medium
surface: /leads/[id] (Contact card + Edit lead dialog)
found_by_run: 2026-08-19T12:00Z
branch: fix/qa-risky-shared-client-and-storage-guard
---

## Summary
A lead's "Contact" card shows the shared `client` record's name/phone/email/postcode instead of this lead's own values (disagreeing with the page's own header), and saving an edit to one lead silently overwrites those fields on the shared client — changing what every OTHER lead attached to that client displays.

## Repro
1. Admin login (any office/admin account) → `/leads/new`. Create lead A with phone/email that match an existing client (dedup matches it — shown as an advisory banner "Matched existing client X").
2. Open lead A's detail page (`/leads/[id]`). The `<h1>` header correctly shows lead A's own `name`. Scroll to the "Contact" card: its **Name** field shows the *other, pre-existing* client's `display_name` — not lead A's name — with no label distinguishing the two sources.
3. Click Edit → change lead A's name → Save.
4. Reload lead B (any other lead sharing the same `client_id`). Lead B's own Contact card "Name" now also shows lead A's newly-edited name, even though lead B's own `leads.name` was never touched.

Concretely reproduced on staging (2026-08-19) with three leads sharing one client:
- `leads.1be79555-...` name = "QA-SENTINEL Admin Lead" → client `b7c4fdaf-...`
- Before edit: `clients.b7c4fdaf-....display_name` = "QA-SENTINEL Estimator Survey Lead" (a DIFFERENT lead's name — from another lead sharing the client)
- After editing lead `1be79555-...` to "QA-SENTINEL Admin Lead EDITED": `clients.b7c4fdaf-....display_name` becomes "QA-SENTINEL Admin Lead EDITED", and lead `7027d61f-...` ("QA-SENTINEL Estimator Survey Lead")'s own Contact card now shows "QA-SENTINEL Admin Lead EDITED" as its Name — despite `leads.7027d61f-....name` being unchanged.
- A third, freshly-created lead (`913ce3ad-...`) that deduped onto the same client immediately displayed "QA-SENTINEL Admin Lead EDITED" as its Name before it was ever edited.

## Evidence
Source, read 2026-08-19:
- `app/(dashboard)/leads/[id]/page.tsx:358` — header: `<h1>{lead.name ?? "Unnamed lead"}</h1>` (per-lead, correct).
- `app/(dashboard)/leads/[id]/page.tsx:475` — Contact card: `<Fact label="Name" value={client?.display_name ?? lead.name} />` (prefers the SHARED client's name over this lead's own `name`; same pattern for Phone/Email/Postcode on the next three lines).
- `app/(dashboard)/leads/actions.ts:374-385` (`updateLeadDetailsAction`) — on every lead save:
  ```
  if (lead?.client_id) {
    await sb.from("clients").update({
      display_name: v.name,
      phone_raw: v.phone || null,
      phone_e164: normalizePhone(v.phone),
      email: v.email || null,
      postcode_home: v.from_postcode || null,
    }).eq("id", lead.client_id);
  }
  ```
  This runs unconditionally whenever a lead is saved, with no check for whether other `leads` rows share `client_id` — a comment above it ("Keep the linked client's core contact aligned with the correction") shows the intent (fixing a bounced/wrong address) but the write has no scope to "this correction only concerns this lead."

SQL used to confirm (service role, staging):
```sql
select id, name, client_id from leads where id in ('1be79555-...', '7027d61f-...', '913ce3ad-...');
select id, display_name from clients where id = 'b7c4fdaf-...';
```
Observed exactly as described above — matches the repro steps.

## Suspected cause
`app/(dashboard)/leads/[id]/page.tsx:475-478` reads client fields as the primary source with lead fields only as fallback (should likely be the reverse — the lead's own captured details are what this lead's page is about); `app/(dashboard)/leads/actions.ts:374-385` writes the client row on every lead edit with no guard for shared clients (should either scope the write to when this IS the only/primary lead on the client, or the UI should make clear it's editing the shared account record, not just this enquiry).

## Verify
1. Re-run steps 1-4 above against a fresh pair of QA-SENTINEL leads sharing one client (dedup by phone/email).
2. Confirm the Contact card's Name/Phone/Email/Postcode on the FIRST lead now show `lead.name`/`lead.phone`/`lead.email`/`lead.from_postcode` (or, if the fix instead scopes the client write, confirm editing lead A no longer changes what lead B's Contact card displays — assert via SQL that `clients.display_name` for the shared client is unchanged after editing only lead A, and lead A's own Contact card still shows lead A's own name).

## Fix (2026-08-22, Peter-directed)

Peter's call: **the lead owns its own details; write through to the shared client
only when this lead is the sole enquiry on it.**

Found while fixing, beyond what the finding recorded: the client-first precedence
was written out by hand at **six** call sites, not one — including the Edit dialog's
own `initial` values (`page.tsx:394-395`). So the damage was never only cosmetic. The
dialog seeded phone and email from the *shared* client, and saving lead B for any
unrelated reason (a notes tweak, a postcode fix) wrote lead A's phone and email into
**lead B's own `leads` row** — the columns comms actually sends to. The Call/Email
action bar dialled the same source.

Both rules now live in one tested module, `lib/leads/shared-client.ts`:

- `leadContact(lead, client)` — the lead's own value wins; the client fills a *gap*
  only. A fallback cannot reintroduce the bug, because it only ever populates a blank
  and so can never contradict a value the page shows elsewhere. All six call sites now
  read one `contact` object resolved once, which is the actual guard: six copies of a
  precedence rule is how the page came to disagree with itself.
- `clientWriteThrough({ clientId, otherLeadCount })` — writes only at
  `otherLeadCount === 0`, and `null` (the count FAILED) declines the write rather than
  coercing to 0.

**Declining is not silent.** `clients.email` is a live sending surface — storage
invoices are addressed from it (`lib/storage/raise-storage-invoices.ts:560`) — so a
correction that stops at the lead has to be reported or the office believes an address
was fixed everywhere. The action returns `{ ok: true, warning }` and the dialog raises
`toast.warning(..., { duration: 12000 })` naming how many other enquiries share the
customer and where to edit it. A successful-but-partial save now reads differently from
a clean one.

**Tests**: 9 new cases in `tests/lib/leads/shared-client.test.ts`. **Mutation-verified**
— reinstating the exact pre-fix behaviour (client-first precedence + unconditional
write-through) fails **5 of 9**; restoring the fix passes 9/9.

Gates: lint 0 errors (36-warn baseline) · tsc 0 · vitest **1885** passed / 7 skipped
(up from 1871 on staging) · build green.

## Still to verify live (why this is not closed)

The Verify above is a live check and has not been run: two QA-SENTINEL leads sharing one
client, edit lead A, confirm via SQL that `clients.display_name` is unchanged and that
lead B's Contact card still shows its own name. The unit tests pin the *policy*; they
cannot prove the action passes the right `client_id` or that the page renders `contact`.
Next audit owns it.
