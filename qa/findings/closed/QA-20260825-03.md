---
id: QA-20260825-03
status: closed
class: risky
severity: high
surface: /quotes/new?leadId=... and any "New quote" action (app/(dashboard)/quotes/actions.ts, createDraftQuote)
found_by_run: 2026-08-25T20:xxZ (admin role agent, gate 1-5 multi-brand audit)
branch: qa/gate6-unskip-brand-ref
---

## Summary
Every quote created for a non-default-brand (Pitmans) lead is denormalised with `quotes.brand = "pitmans"` correctly, but its `quote_ref` is always minted with the **Marley** prefix (`MMR###`/`MMC###`) instead of Pitmans' (`PMR###`/`PMC###`) — the brand chip, filter, and `brand` column are right; the customer-facing reference number (and the subject line/PDF/bank-reconciliation trail built from it) is wrong.

## Repro
1. Role: admin (staging).
2. Have (or create via `/leads/new` picking "Pitmans") a lead with `brand='pitmans'`.
3. Navigate to `/quotes/new?leadId=<that lead id>` (or click "New quote" from the lead page) — this creates a draft quote via `createDraftQuote`.
4. Expected: `quote_ref` starts with `PM` (migration 0104's own comment: "next_quote_ref can mint MMR042 and PMR001 from the same code path").
5. Actual: `quote_ref` starts with `MM`.

## Evidence
Reproduced live on staging, 2026-08-25, by two independent parties:

Role agent (browser-driven, 3x): 
- Lead `8b46b7e3-36b5-4f2e-9e1a-d6ea6587bc50` (`leads.brand='pitmans'`) → quote `2af2b85a-e0be-4121-9464-01b1825d3e28`: `{"brand":"pitmans","quote_ref":"MMR085"}`
- Lead `c37bfce6-95ab-48ce-8fd2-1e3951827159` (`leads.brand='pitmans'`) → quote `cb65f8a4-164b-44ed-bbc3-33353f11535f`: `{"brand":"pitmans","quote_ref":"MMR086"}`, and its revision quote `28c6a5fb-14e9-43d9-ae32-be4fd0bbf352`: `{"brand":"pitmans","quote_ref":"MMR087"}`
- The sent email's own subject line surfaced the mismatch: `"Your removal quote from Marley Moves — MMR086"` for a Pitmans quote.

Main-loop independent spot-check (RPC-level, not browser-driven — confirms the exact root cause, not just the symptom):
```
next_quote_ref(kind: "R")                    -> MMR088   (app's actual call shape — no brand arg)
next_quote_ref(kind: "R", brand: "pitmans")   -> PMR001   (RPC already supports this; just isn't asked)
```

## Suspected cause
`app/(dashboard)/quotes/actions.ts:44`, inside `nextQuoteRef()`:
```ts
const { data, error } = await sb.rpc("next_quote_ref", { kind });
```
never passes `brand`, so it always falls through to the RPC's `brand text default 'marley'` (migration `0104_brands.sql:214`) — even though `createDraftQuote` already resolves `lead?.brand ?? DEFAULT_BRAND` two lines below for the `brand` column write (`quotes/actions.ts:165`). The fix is passing that same resolved brand into the RPC call: `sb.rpc("next_quote_ref", { kind, brand })`.

Downstream effect: Pitmans' `brand_ref_counters` row (`kind IN ('R','C')`) is never incremented — it stays at 0 indefinitely while Marley's counter absorbs both brands' quotes — which will also eventually collide with the "PMR001 already issued to a customer, never reissue" invariant documented in `0104_brands.sql` once this is fixed naively. A correct fix must NOT reissue any `MMR###` ref already sent to a Pitmans customer under this bug — flagging as `risky` for that reason, plus its bank-reconciliation and quote-ref-immutability implications (`leads/actions.ts` documents the ref is fixed once issued specifically so it can be trusted downstream).

## Verify
Create a quote for a lead with `brand='pitmans'` (via `/quotes/new?leadId=...` or "New quote") and confirm `select brand, quote_ref from quotes where id='<new id>'` returns a `quote_ref` matching `^PM[RC]\d+$` for `brand='pitmans'` (and still `^MM[RC]\d+$` for `brand='marley'`). Also confirm no already-issued `MM`-prefixed ref belonging to a Pitmans quote gets silently reissued/changed by the fix — those rows keep their existing (wrong-prefix but already customer-facing) ref; only NEW quotes mint correctly going forward, unless Peter decides otherwise.

## Additional evidence — downstream symptom in gate 14's brand-prefixed PDF filenames (2026-08-26)
Gate 14 (commit `daddb44`, PR #105) landed brand-prefixed quote PDF filenames (`Pitmans-Quote-<ref>.pdf`) on the assumption the ref is already correctly minted. Live role-agent check on staging (sha `c01be21`) confirms the exact glued-together symptom this finding predicted: a fresh Pitmans-brand quote (`quotes.brand='pitmans'`, `quote_ref='MMR101'`) downloaded as **`Pitmans-Quote-MMR101.pdf`** — the "Pitmans-" prefix (gate 14, correct, keyed off `quotes.brand`) glued to an MM-prefixed ref (this finding's bug). PDF content itself was independently verified correct otherwise (Pitmans wordmark, blue `#2B2B76` accent, correct legal line, shared `MARLEYMOVES LTD` bank details) — the filename's ref segment is the only artefact of this bug visible in gate 14's surface. No new finding filed; this is the same root cause, just a newly-landed place it shows up. Once this finding's fix lands, gate 14's filenames self-correct with no further change needed (the filename logic already reads `quotes.quote_ref` directly).

## VERIFIED CLOSED — 2026-08-26T10:0xZ (post-merge of PR #90, gate 6)

Closed verify-first against LIVE staging (`https://staging.ops.marleymoves.co.uk`,
staging sha `a0935f0`), not on the strength of the merge.

**How it was proven.** The purpose-built spec for this finding —
`e2e/office/quote-brand-ref.spec.ts` — already existed but was **skipped** behind a
`KNOWN_BUG_QA_20260825_03 = true` guard, so every green e2e badge since it landed said
nothing whatever about this bug. Deleting that guard IS the close: the assertion now runs.
It drives the real app path (`/quotes/new?leadId=...` → `createDraftQuote`), not the RPC
directly, which matters because the defect was in the app layer, not the DB function.

```
$ npx playwright test e2e/office/quote-brand-ref.spec.ts --project=office
  ▶ E2E target: https://staging.ops.marleymoves.co.uk
  ok 4 [office] › quote-brand-ref.spec.ts:86:7 › a pitmans-brand lead's new quote
       gets a PM-prefixed ref, not MM (2.3s)
  4 passed (12.0s)
```
Asserted in that spec: `quote.brand === "pitmans"` AND `quote.quote_ref` matches
`/^PM[RC]\d+$/`.

**Counter evidence (the half a passing assertion does not show).** Read directly from
`brand_ref_counters` on staging, before and after the run:

| brand | kind | before | after |
|---|---|---|---|
| pitmans | R | 5 | **6** |
| marley | R | 122 | **122** |

The Pitmans counter advanced and **Marley's did not move** — so the Pitmans mint is drawing
from its own sequence rather than borrowing Marley's, which is the actual behaviour this
finding was about. PMR006 is burned (the spec deletes its quote; counters never reset). That
is expected and acceptable on staging.

**Teardown.** Orphan sweep after the run: 0 rows in `leads`, 0 in `clients` for marker
`E2E-QUOTE-BRAND-REF`; 0 surviving PM-prefixed quotes.

**The "never reissue an already-sent MMR ref" half is verified by CODE PATH, not by data,
and that distinction is deliberate.** The original evidence rows (quotes MMR085/086/087 on
leads `8b46b7e3` / `c37bfce6`) no longer exist — the e2e seed reset staging's known state —
so there is nothing left to observe being preserved. What can be stated: the fix only
supplies a `brand` argument to `next_quote_ref` at MINT time inside `createDraftQuote`;
nothing in PR #90 reads, rewrites or backfills an existing `quotes.quote_ref`, so an
already-issued ref cannot change. A live re-check of the preservation behaviour would need
prod data, and prod has no Pitmans quotes. Recorded as inspected rather than observed so a
later reader does not mistake this for a measured result.
