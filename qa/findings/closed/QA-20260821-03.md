---
id: QA-20260821-03
status: closed
class: safe-fix
severity: low
surface: /my-jobs/[id] (crew job detail — Vehicles card)
found_by_run: 2026-08-21T04:18Z
branch: qa-repair/QA-20260821-03
---

## Summary
On the crew job-detail page, when an appointment's lead has no accepted quote, the "Vehicles" card renders the *packing*-label fallback text "See quote" as its subtitle — which reads as an instruction about vehicles, not packing.

## Repro
1. Crew login → open `/my-jobs/<id>` for a removal appointment whose lead has no `quotes` row with `status='accepted'`.
2. Look at the "Vehicles" card.
3. Expected: either nothing renders under "Vehicles" when there's no vehicle info, or any packing-related fallback text is scoped to a packing-labelled area, not the Vehicles card.
4. Actual: the card renders "VEHICLES" (heading) → "See quote" (subtitle) → "No vehicles assigned yet…". "See quote" is `packingLabel`'s no-quote fallback, not vehicle info.

Concretely on staging (2026-08-21), a QA-SENTINEL marker removal appointment with no accepted quote rendered the job-detail page text exactly:
`"VEHICLES / See quote / No vehicles assigned yet — the office will assign them…"`.

This isn't only a seeded-marker edge case: per the current changelog, iMVE-imported/legacy bookings can legitimately have no accepted quote at all, so this fallback is reachable on real jobs.

## Evidence
Source, read 2026-08-21:
- `lib/job-sheet-data.ts:100-101` —
  ```ts
  vehicleLabel: q ? vehicleLabelOf(q) : "",
  packingLabel: q ? (PACK_LABELS[q.packing] ?? q.packing) : "See quote",
  ```
  When there's no accepted quote (`q` is null/undefined), `vehicleLabel` is `""` (correctly renders nothing) but `packingLabel` falls back to the string `"See quote"` — a packing-specific fallback with no quote-independent home in the UI.
- `app/my-jobs/[id]/page.tsx:244-246` — the "Vehicles" card renders `d.vehicleLabel` then, directly under it, `d.packingLabel`:
  ```tsx
  {eyebrow(<Truck className="size-3.5" strokeWidth={1.75} />, "Vehicles")}
  {d.vehicleLabel ? <p className="mt-1.5 text-sm font-medium text-foreground">{d.vehicleLabel}</p> : null}
  {d.packingLabel ? <p className="text-xs text-mist-400">{d.packingLabel}</p> : null}
  ```
  There is no `d.vehicleLabel` set, so the `d.packingLabel` line ("See quote") appears alone as the Vehicles card's subtitle.

## Suspected cause
Two-hats-adjacent: `packingLabel`'s no-quote fallback text ("See quote") was written assuming it always renders in a packing-labelled context, but it's reused as the Vehicles card's subtitle whenever `vehicleLabel` is empty, with nothing to disambiguate which field it's talking about.

## Verify
On `/my-jobs/<id>` for an appointment whose lead has no accepted quote, assert the Vehicles card either shows nothing (no subtitle) or does not display packing-fallback text ("See quote") as if it described vehicles.

## Fix
Fixed at the data layer, not the page: `lib/job-sheet-data.ts`'s no-quote
`packingLabel` fallback changed from `"See quote"` to `""` — the string told
crew to see a quote that does not exist, and it rendered wherever the label
lands (the /my-jobs Vehicles card per this finding, plus the day sheet and
`/sheet/[token]` "Packing" rows). Every consumer already guards on emptiness
(`d.packingLabel ? … : null`, `s.packingLabel ? [s.packingLabel] : []`,
`data.quoteRef ? data.packingLabel : null`), so an empty label renders
nothing everywhere — the finding's "Expected" outcome — with no page markup
touched. One companion change: the job-sheet PDF's JOB SPEC line
(`lib/job-sheet-docdef.ts`) joined `vehicleLabel  ·  packingLabel`
unconditionally, which with empty labels would leave a dangling `·`; it now
joins only the non-empty parts. Safe: display-only, two pure modules, no
data/comms/money paths.

Tests (`tests/lib/job-sheet.test.ts`, both verified to fail pre-fix): the
no-quote assembly case now asserts `packingLabel`/`vehicleLabel` are empty,
and a new docdef case asserts the no-quote sheet contains no "See quote" and
no half-empty separator while the with-quote spec line still reads
"vehicle · packing". Gates: lint 0 errors (36-warning baseline) · tsc 0 ·
vitest 1806 · build green.

## Verified

2026-08-21 (~12:15 UTC), against deployed staging (`f574d61`, `/api/version`
matched). Confirmed `lib/job-sheet-data.ts`'s no-quote `packingLabel`
fallback reads `""` in the current checkout (line 104), with the comment
explaining why it's never a placeholder string.

Live repro: minted a throwaway crew user (`QA-SENTINEL Crew` full_name,
random password) + one marker removal appointment (client + lead, **no**
`quotes` row, crew assigned via `appointment_assignments`), all carrying
`QA-SENTINEL` in name/notes fields. Logged in as that crew user with
Playwright (Chromium at `/opt/pw-browsers/chromium`,
`--ssl-version-max=tls1.2` + `--proxy-server=$HTTPS_PROXY`), loaded
`/my-jobs/<appointmentId>`, and read the Vehicles card's full text:

```
"VEHICLES\n\nNo vehicles assigned yet — the office will assign them; call the office if this is still empty on the morning of the move."
```

No subtitle line between the heading and the empty-state message — "See
quote" does not appear anywhere on the card (or the page). Matches the
finding's Expected outcome exactly.

Cleanup: all marker rows deleted and re-verified gone by targeted ID query
(leads/clients/appointments/staff/profiles/auth user all confirmed absent).
Moving to `closed/`.
