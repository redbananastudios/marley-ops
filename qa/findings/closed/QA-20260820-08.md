---
id: QA-20260820-08
status: closed
class: risky
severity: medium
surface: /leads/[id] (Delete lead button) → lib/leads/deletable.ts "it has a storage let" guard
found_by_run: 2026-08-20T13:24Z
branch: fix/qa-risky-shared-client-and-storage-guard
---

## Summary
The lead-delete guard's "it has a storage let" protection is effectively dead code for real data: `canDeleteLead` counts `storage_lets` by `.eq("lead_id", id)`, but the only real INSERT path (`startLetAction` in `app/(dashboard)/storage/actions.ts`) never sets `storage_lets.lead_id` — it only takes a `client_id`. Every genuine storage-business display surface (`/storage`, `/performance`, `/s/[token]`) also keys storage lets to a customer by `client_id`, never `lead_id`. So a lead whose client has a real, active storage unit under contract can pass the delete guard freely, because the count the guard reads is always 0 in practice.

## Repro
1. Service-role SQL, staging, 2026-08-20: `select column_name, is_nullable from information_schema.columns where table_name='storage_lets'` — `client_id` is `NOT NULL`, `lead_id` is nullable.
2. Grep `app/(dashboard)/storage/actions.ts` `startLetAction` (the only INSERT into `storage_lets` in the app): the insert payload sets `unit_id`, `client_id`, dates, rate — never `lead_id`.
3. Grep the three real display surfaces — `app/(dashboard)/storage/page.tsx:28`, `app/(dashboard)/performance/page.tsx:102`, `app/s/[token]/page.tsx:37` — all query `storage_lets` by `client_id`.
4. `app/(dashboard)/leads/actions.ts` `deleteLeadAction`'s `COUNTED` array checks `storage_lets` by `.eq("lead_id", id)` — for any storage let created through the real UI, this is 0, so `lib/leads/deletable.ts`'s `if (f.storageLets > 0) return { deletable: false, reason: "it has a storage let" }` never fires for genuine storage business.

## Evidence
Live-verified mechanically (a manually seeded `storage_lets` row with `lead_id` explicitly set correctly triggers the refusal — the column match itself works), but the population path that would make that count non-zero in real usage does not exist anywhere in the app.

## Suspected cause
`storage_lets.lead_id` is a nullable, effectively-vestigial column that the storage feature was never wired to populate; the delete guard was written assuming it would be.

## Why risky, not safe-fix
This is a business-history judgement call, not a mechanical bug: is a lead-level check even the right model for a client-level object like storage (a storage let deliberately outlives any one lead/enquiry)? The fix could be "count storage_lets by client_id instead" (broadens the refusal to ANY lead on a client with a storage let, which may be too broad — one of several leads on a client shouldn't necessarily be blocked from deletion by a sibling's storage business) or "populate lead_id on let creation" (a data-model change) or "leave as-is, storage isn't really lead-scoped so the guard's intent doesn't apply." Needs Peter's call, same class of decision as the money-classification findings (QA-20260820-03/-04).

## Verify
After Peter's decision on the intended model: seed a lead whose client has an active storage let (created via the real UI path, not a hand-set `lead_id`), attempt delete, and assert it is refused with a "storage let" reason (or that the chosen alternative behaviour holds).

## Fix (2026-08-22, Peter-directed)

Peter's call among the three options this finding set out: **count by `client_id`, but
refuse only when this is the LAST lead on that client.**

The reasoning that picks that option over "count by client_id always": the storage let
itself is never at risk. `storage_lets.client_id` is `NOT NULL` and `deleteLeadAction`
does not delete the client, so the storage business survives a lead delete either way.
What a delete can destroy is the **enquiry behind** the let — and a surviving sibling
lead already preserves that trail. Blocking every lead on the client would make an
obvious typo duplicate undeletable purely because a sibling has storage business, which
is the opposite of what delete exists for.

- `storageLetsBlockingDelete({ clientId, siblingLeadCount, letsOnClient })` in
  `lib/leads/deletable.ts` — pure, so the policy is testable where the old one was not.
- `LeadDeletionFacts.storageLets` renamed to **`orphanedStorageLets`**, because the
  number means "lets this delete would strand", not "lets on this lead". The old name is
  what made a dead check look correct for four months.
- Refusal message is now "it is the last enquiry behind a storage let" — it names the
  real reason, so the office can tell it apart from "this lead has storage".
- `deleteLeadAction` hoists the sibling query above the verdict (it was already run
  below, for the merge) and now **fails closed** on a sibling read error, which it did
  not: an ignored error read as "no siblings", which would both skip this guard and lose
  the history the delete is meant to merge. The `storage_lets` count is skipped entirely
  when a sibling exists, since it cannot change the answer.

**Tests**: 5 new cases in `tests/lib/leads/deletable.test.ts`. **Mutation-verified** —
reducing the helper to the old effective behaviour (`return 0`, the dead guard) fails
**2 of 15**; restoring the fix passes 15/15.

Gates: lint 0 errors (36-warn baseline) · tsc 0 · vitest **1885** passed / 7 skipped ·
build green.

## Still to verify live (why this is not closed)

The Verify above needs a real storage let created through the UI path (`startLetAction`,
which sets only `client_id`) on a client whose lead is the last one, then an attempted
delete refused with the new reason — plus the negative case, that a lead with a sibling
still deletes. The unit tests pin the policy, not the wiring. Next audit owns it.

## Closed — verified live 2026-08-22T20:40Z against deployed staging `ae47fc0`

Seeded a `storage_lets` row matching `startLetAction`'s exact real insert shape
(`client_id` set, `lead_id` never set — confirmed unchanged by this fix, re-grepped
`app/(dashboard)/storage/actions.ts`) on a client with exactly one lead.

- **Sole-lead case**: attempted delete via the real `/leads/[id]` UI. Refused with the
  toast *"This lead can't be deleted because it is the last enquiry behind a storage let.
  Mark it lost instead."* — the exact new reason text. Lead confirmed still present in
  `leads` afterward.
- **Sibling case**: added a second lead to the same client (a genuine duplicate scenario),
  re-attempted delete on the original lead. Succeeded — toast *"Duplicate deleted, its
  history was kept"* — and SQL confirmed the lead was actually removed. A surviving
  sibling correctly un-blocks the delete, as designed (the enquiry trail survives on the
  sibling; the storage business survives on the client either way).

Both halves of the original Verify section hold. Fixtures (site/unit/let/clients/leads)
cleaned up, verified zero by query.
