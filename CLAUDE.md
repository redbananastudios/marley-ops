@AGENTS.md

## Current State

The premium role-based UI redesign (worktree `O:\projects\red-banana\clients\marley\marley-ops-ui-workflows`, branch `codex/premium-role-ui`) is **feature-complete through rollout pass 4** — pass 4 (`4ce9b10`…`35eabbb`) added: brand-mark centring, the sidebar-scroll-to-Settings + header-hairline fixes, the cubic category-chip wrap fix (/cv + office builder), the customer routes `/q` `/cv` `/s` brought into the design system (local logo, cool tokens, Cormorant headings), and a full lint cleanup so **`npm run lint` now exits 0**. Details below reflect through pass 3 — browser-accepted at all three role viewports, committed locally (`1fe1be4` … `83b88b0`, **NOT pushed**), production `npm run build` green, 278 tests + tsc green. Delivered: shared black-rail shell + role-aware nav; the redone brand lockup (official full-colour Marley brandmark in a white app-tile + "MARLEY OPS" in **Cormorant Garamond** via next/font); crew sticky cab action bar; persistent autosave feedback on the quote + cubic builders; calmed colour vocabulary; Bookings aligned to the shared shell; and one unified tab/filter vocabulary (`components/ui/segmented.tsx`) with red removed from every active tab/filter (reserved for CTAs + the sidebar marker). Only out-of-scope items remain (customer-facing `/q` `/cv` `/s` routes; a separate pre-existing-lint cleanup) — see `docs/ui-role-workflows-handoff.md`. Local dev: Supabase Docker stack + `scripts/seed-dev.mjs` / `seed-dev-crew.mjs` (crew fixtures left in place for review); keep `SANITY_SYNC_DISABLED=true` in `.env.local` (the dashboard otherwise syncs REAL leads from prod Sanity into dev). Do not edit the primary checkout.
