@AGENTS.md

## PRODUCTION IS LIVE (cutover 2026-07-30, Peter's order)

**`ops.marleymoves.co.uk` is the LIVE system of record since 2026-07-30 12:09 UTC.** Real customers, real money, real emails (`COMMS_DRYRUN=false`), card payments LIVE (takepayments merchant 292748, kill switch ON), Zoho = Connor's real books. Treat every prod write as customer-facing. The go-live flush ran via `scripts/reset-data.mjs`; `LEAD_SYNC_SINCE=2026-07-30T12:09:26Z` is the no-backfill floor — never remove it (historical website leads must not import). **Dev stays guarded**: `SANITY_SYNC_DISABLED=true` remains in `.env.local` so dev never pulls real enquiries (2026-07-13 lesson: the Sync button re-imported 78 real enquiries into dev mid-test).

## AI survey gotchas (2026-07-13)

- **`GEMINI_API_BASE_URL` MUST include `/v1beta`** (`https://generativelanguage.googleapis.com/v1beta`). `lib/ai/gemini.ts` polls file status at `${baseUrl}/${file.name}` and passes baseUrl into `createGoogle` — the bare origin 404s every analysis ("Gemini file status failed (404)"). Only the upload path tolerates both forms. Prod `app.env` fixed to the /v1beta form 2026-07-13. The intended pre-launch policy is `COMMS_DRYRUN=true`, but live `app.env` was verified as `COMMS_DRYRUN=false` on 2026-07-20; do not assume sends are simulated or change the flag without Peter's cutover decision.
- **Local dev has NO cron** — `ai_jobs` sit `queued` forever and the survey UI polls indefinitely. Drain manually while logged in as office: open `http://localhost:3015/api/cron/ai-jobs`.

## House conventions

- **Before ANY push: `npm run lint` locally, always** — the CI gate enforces ESLint
  rules tsc never sees (react-hooks, no-unused-vars, no-unescaped-entities). Running
  only tsc+vitest has now broken the pipeline twice (session 32 agents; 2026-07-22
  balance refactor). All four gates or it doesn't ship: lint, tsc, vitest, build.

- **Page shell (2026-07-16, Peter caught /content hugging the edge):** every `app/(dashboard)/**` page's top-level element must be `<main className="flex-1 p-6 md:p-8">` (or the deliberate `page-shell` variant used by the dashboard/estimator views). The shared layout adds NO padding on purpose — a bare `<div>` root renders flush against the viewport. Full 34-page audit passed 2026-07-16; keep it true for new pages.

## Current State

Last touched: 2026-07-30 on i9 — **SYSTEM WENT LIVE (cutover 12:09 UTC).** Pre-flush backup 12:51-prior + nightly; full flush (DB + Supabase Storage + R2; identity/config kept; reset-data.mjs composite-PK fix shipped); live users peter/luke/connor/leanne@marleymoves.co.uk (shared launch password, Peter distributes — no credential-email feature exists by design), test logins deactivated, Crew Test staff retired; `LEAD_SYNC_SINCE=2026-07-30T12:09:26Z` + `SANITY_SYNC_DISABLED` removed (sync proven ok/0 — no backfill); takepayments flipped LIVE (merchant 292748, TEST_MODE=false, kill switch ON, health "Merchant ••••2748 · LIVE"); Zoho verified clean (only app test doc INV-000191 already void — the org is Connor's REAL books, actively used); fleet-reminder recipients set (connor@+luke@); all 13 crons wired; browser-verified fresh login (Leanne) + all-green integration health. Remaining = Peter: £1 real-card test + refund proof, WhatsApp sign-up link, drivers via Staff & Fleet, VAT stagger, T&Cs, iMVE parallel. Details: `docs/go-live-checklist.md` C-block. **Evening cleanup:** the TEST enquiry Peter used to exercise the payment flow was purged end-to-end (Zoho INV-000200 void+DELETE + TEST contact removed from Connor's books; 11 DB rows + client; the card payment was pending-only, never charged — nothing to refund; MMR012 consumed, next ref MMR013) and **quote emails now send from accounts@** (`bc14b87` — overrides the identity plan's owner-fronting for the quote email; deposit chases still owner-fronted).

Prior session (2026-07-29) — **SCHEDULE & ALLOCATION SHIPPED (`e6e058f`, merged `3d8c67a`): one page, two views of the same day; migration 0080 on prod.**
- **New `/schedule` page (additive — Removals diary + Job Board untouched, retire later):** *Availability* tab = month graded from the confirmed removals' REQUIRED vans/crew vs the live fleet (Available/Limited/Full/Over; pure `lib/schedule/capacity.ts`, 14 tests, `limitedAt` threshold configurable) + soft-demand "Thinking about it" panel (deposit-paid, off-diary) + click-a-day summary (booked jobs, provisional interest, ±3 days). *Day Allocation* = Job Board single-day for dispatch, surveys off, shared `?date=`.
- **Bookings from the page:** New booking prefilled to the selected date; clicking a booked job opens the diary's view modal → Edit / Reschedule (booked removals via the Payments Policy v2 ChangeDateDialog). All flows browser-verified as office.
- **Driver-aware crew:** `staff.is_driver` (Staff & Fleet toggle, admin-only write) → charcoal+car-mark rail chips, warn-only "No driver assigned" on job cards (never blocks, mirrors clash warnings).
- **Migration 0080 APPLIED + VERIFIED ON PROD** (additive, no backfill): `booking_details` side-table (approx window/month, provisional date, homeowner/rented; 4 RLS policies) + `staff.is_driver`. Date-certainty is DERIVED from existing deposit/25%/appointment signals — the full multi-move `bookings` entity is a deliberate later refactor (design doc §4).
- **Design + phases:** `docs/schedule-allocation-design.md` (D1–D14 locked with Peter). Fast-follow this week: first-class packing days, single shared booking drawer (adds the approx-window/property capture UI), Bookings-page money-queue re-group, retire old pages when happy.
- **Batch 2 same day (`15c4436`, migration 0081 on prod):** PUBLIC crew sign-up — one shared `/join/<token>` link (WhatsApp-able; mobile-first; token+kill-switch+honeypot+25/hr cap; service-role-only writes, staff_submissions has NO insert policy) + Staff & Fleet admin card (copy link / enable / regenerate) + review queue (Approve = create-or-update staff with normalised-phone dedupe, name-mismatch refusal, update-existing confirm, pending→approved mutex). Staff gains address/DOB/emergency columns. AND the **shared booking-details drawer** — single writer for booking_details (approx window, target month, provisional date today..+2y, homeowner/rented; money display-only) from lead record / Bookings / schedule panel + day summary. Workflow-built (2 builders + 2 adversarial reviewers), all 6 findings fixed, both flows browser-verified. **Kill switch defaults OFF on prod — Peter enables in Staff & Fleet when ready to recruit.**
- **Open carried:** PCI SAQ 8.3.6/8.3.7/8.3.9 mis-attestation + password policy — ClickUp 869eb591y, due 30 Sep (detail: changelog 2026-07-29 PCI entry + memory [[marley-pci-compliance]]).
- **Email copy pass (same day, with Peter 1-by-1):** every automated email reviewed against its live copy. EM-DASHES REMOVED from ALL customer-facing email copy (all 21 Resend templates rewritten + republished, incl. the site's lead-auto-reply; app-side subjects/fallbacks/Zoho invoice notes mirrored; signed date-confirm ack reworded pre-go-live). **Card is now offered on every money surface** ("card over the phone on 01747 637070") — storage invoices + Zoho notes, commitment chase, balance invoice, date-confirmation block; online card button stays deposit-only. Storage footer rephrased professional ("Whenever you're ready to arrange collection, please get in touch and we'll book it in."). Template script hardened: `--only <name>` flag + pagination fix (`?limit=100` — the 20-per-page default silently DUPLICATED the 21st template; survey-confirmation repaired in place, dup deleted). Resend template copy ≠ repo fallback was a live drift — both now aligned.
- Gates: tsc 0 · lint 0 · vitest 1195 · build green.

_Prior sessions → brain `O:\brain\01_Projects\Marley Moves\marley-ops CHANGELOG.md` (full "Last touched" history, newest-first; query via `/recall`). This block holds the latest session only — `/ur` evacuates older blocks there. Deployment/ops runbook: `docs/ovh-deployment.md`; go-live checklist: `docs/go-live-checklist.md`._
