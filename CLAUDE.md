@AGENTS.md

## Data policy until go-live (Peter, 2026-07-13)

**Production `ops.marleymoves.co.uk` holds MOCK/TEST data only until Peter approves testing complete.** No live-lead backfill, no real customer records, keep `SANITY_SYNC_DISABLED` in place. The `growth_artifacts` rows are exempt (agent proposals + tracking summaries — no customer data). Do not flip anything to live data without his explicit approval. **Dev mirrors this**: `SANITY_SYNC_DISABLED=true` is set in `.env.local` too (2026-07-13 — the leads-page Sync button re-imported 78 real website enquiries into dev mid-test; wiped + guarded).

## AI survey gotchas (2026-07-13)

- **`GEMINI_API_BASE_URL` MUST include `/v1beta`** (`https://generativelanguage.googleapis.com/v1beta`). `lib/ai/gemini.ts` polls file status at `${baseUrl}/${file.name}` and passes baseUrl into `createGoogle` — the bare origin 404s every analysis ("Gemini file status failed (404)"). Only the upload path tolerates both forms. Prod `app.env` fixed to the /v1beta form 2026-07-13. Also `COMMS_DRYRUN=true` on prod until go-live (mock-data policy) — flip to `false` at launch.
- **Local dev has NO cron** — `ai_jobs` sit `queued` forever and the survey UI polls indefinitely. Drain manually while logged in as office: open `http://localhost:3015/api/cron/ai-jobs`.

## House conventions

- **Page shell (2026-07-16, Peter caught /content hugging the edge):** every `app/(dashboard)/**` page's top-level element must be `<main className="flex-1 p-6 md:p-8">` (or the deliberate `page-shell` variant used by the dashboard/estimator views). The shared layout adds NO padding on purpose — a bare `<div>` root renders flush against the viewport. Full 34-page audit passed 2026-07-16; keep it true for new pages.

## Current State

Last touched: 2026-07-20 on i9 (latest) — **E2E COVERAGE COMPLETE — role-based Playwright suite now green across all four roles; the last two depth items shipped (PR #14, live prod `dace49f`, CI test gate + OVH deploy green).**
- **Storage** 🟡→✅ (`e2e/office/storage.spec.ts`): the FULL let-assignment chain — create site → select it → add unit → assign the seeded client at £25/wk → the unit flips to Occupied showing client + rate (the let dialog only offers existing clients, so it uses the seeded `E2E Storage Client`).
- **Claims** (`e2e/office/claims.spec.ts`): the full arc on one seeded open claim — advance non-terminal (open→assessing) → settle terminally, which reveals the required resolution + amount (goodwill payment → £150) → verify the settled Outcome + payout render.
- Full `--project=office` run **0 failures** (58 passed / 6 skipped) after restoring the dev server's staging-Zoho env — the lone earlier `p0-money` failure was that env drift (the :3016 server started without `.env.e2e`), NOT a regression; re-ran green with the staging invoice auto-purged by `globalTeardown`. code-reviewer verdict SAFE; its one MEDIUM — the settle could race a stale `updated_at` CAS token on a slower target — fixed with a reload gate (server-confirms persistence + refreshes the token) before merge. `e2e/COVERAGE.md` now all-✅ for the office surfaces.
- **E2E harness reminders:** chromium-only, serial (`workers:1`, `retries:0`), `global-setup` HARD-REFUSES prod (`ops.marleymoves.co.uk`); env-agnostic via `E2E_BASE_URL`; known state via `scripts/seed-e2e.mjs` (fixtures in `e2e/fixtures/seed-data.ts`, one manual seed at suite start, mutations accumulate in alphabetical file order). Staging Zoho = Demo Removals org `20117092566` (creds in gitignored `.env.e2e`, never Connor's live books). This wave (PRs #12→#14) = every function/feature, every role, via Playwright. Run: reseed then `E2E_BASE_URL=http://localhost:3016 npx playwright test --project=office`.
- Prior:

Last touched: 2026-07-20 on i9 — **Estimator-commission low money edges CLOSED (`c44a7b6`) — the last open items on the estimator-invoicing review. ClickUp [869e6h9ex](https://app.clickup.com/t/869e6h9ex) fully closed.** Pure engine + a warning toast, no DB change; +5 unit tests, 729 green, shipped via CI.
- **(1) Multi-accepted-quote pick** now deterministic — most-recently accepted, tie-broken by quote_ref (matches /bookings + /performance) + a warning if >1 accepted survives (supersede-guard miss).
- **(2) Both-null-value accepted quote** no longer silently drops the £0 commission — the completed job is skipped with a surfaced warning ("no agreed price — ask the office to set it") so the estimator is never quietly short.
- **(3) VAT registration-date floor** — new pure `jobVatApplies()` reuses `lib/finance` `VAT_REGISTERED_FROM` (2026-06-01); a job billed before registration is treated as ex-VAT (no /1.2 on a stale `vat_enabled=true` flag → no under-pay). Uses the completion (billing) day.
- Prior:

Last touched: 2026-07-20 on i9 — **MED-4 CLOSED: Staff & Fleet record writes are ADMIN-ONLY (`8a89e5f`, migration 0067). The 2026-07-20 audit is now FULLY closed — no open findings.** Prod RLS-simulated (rolled back); shipped via CI.
- **The hole:** `staff` INSERT/UPDATE (0054) gated on `is_office()`, but estimators ARE `is_office()`, and `staff.profile_id` is an auth key the availability/pay-statement/`staff_pay` self-scopes trust — so an estimator could repoint any staff row's profile_id to themselves and hijack another person's availability/pay/rate reads. Bounded after 0065 (not a self-payment vector) but a real identity hijack.
- **Fix (Peter's call — admin-only Staff & Fleet):** migration 0067 moves `staff` + `vehicles` insert/update to `is_admin()`; the crew/estimator first-login **self-link branches are preserved verbatim** (claim your own email-matched unlinked row / edit a row already yours — you just can't repoint profile_id at anyone else or touch foreign rows). App layer: `saveStaff`/`setStaffActive`/`saveVehicle`/`setVehicleActive` + the two vehicle off-road actions → `requireAdminActor` (retired the loose `actor()` helper); **staff-availability scheduling stays office** (estimators plan around crew). UI hides Add staff/Add vehicle + per-card edit/archive + the wall-chart edit-staff pencil for non-admins; availability cells stay editable by office.
- **Prod RLS-proven (rolled back):** estimator Luke — hijack Jack 0 rows, insert staff/vehicle BLOCKED, vehicle update 0 rows, but own-row self-manage 1 row; admin Connor — all allowed; crew Jack — own-email self-link 1 row, hijack Rob 0 rows. tsc + lint(0 err) + **724 tests** + build green.
- **Note (capability change to flag):** estimators (Luke) can no longer add/edit crew or van records, archive them, or book a van off-road — that's now admin (Connor/Peter) only. Realistically already how it worked. Reversible if you want estimators back in fleet management.
- Prior:

Last touched: 2026-07-20 on i9 — **HARDENING BACKLOG CLEARED: M2 + M5 SHIPPED (`820744c`), the whole 2026-07-20 sweep now closed.** Deployed via CI (GH Actions healthy again — test + deploy both green); `/api/version`=820744c verified live.
- **M2 (storage-billing silent under-bill):** a transient Zoho failure left the just-claimed `storage_invoices` row behind and `periodsDue()` (status-agnostic) then treated that period as billed forever. The catch now **DELETEs the still-`pending` claim** (orphan adoption on the stable `MMS-<let>-<period>` reference keeps the re-create idempotent even if Zoho did make the invoice), and one **consolidated money ops-alert** fires per run. No period can silently skip.
- **M5 (PostgREST 1000-row truncation):** seven unbounded full-table reads → `fetchAllRows` with a stable `.order(...).range()` — dashboard leads/appts/quotes (leads keeps `submitted_at`-desc + `id` so the "Latest enquiries" `slice(0,6)` is unchanged), bookings accepted-quotes, survey-diary appts/leads (compound display order + `id` tiebreaker — the picker reads the array directly), performance storage_invoices/leads/quotes, weekly-digest leads/quotes/appointments (the `.limit(2000)` was a lie — PostgREST caps at 1000), crew-reminders un-reminded assignments. NOT expanded: bookings' `.in(leadIds)` reads (a distinct `.in()`-batching concern, years out) and completions' genuinely week-windowed `.limit(1000)`.
- tsc + lint(0 err) + **724 tests** + build green. ClickUp [869e6hwbj](https://app.clickup.com/t/869e6hwbj) closed. Remaining audit tail was the deferred RLS **MED-4** — **now also closed** in `8a89e5f` (see the newer block above). Full sweep detail in memory [[marley-hardening-sweep]].
- Prior:

Last touched: 2026-07-20 on i9 — **MULTI-AGENT HARDENING SWEEP → cutover-gate cluster SHIPPED (`ffd666a`, migration 0066). Deployed via the MANUAL fallback (`scripts/deploy-ovh.sh`) — GitHub Actions was mid-incident (500/503, dispatch stalled); our OVH runner was healthy+idle, the stuck run was cancelled, box built with the correct GIT_SHA stamp. `/api/version`=ffd666a verified.**
- Ran a Workflow of **8 dedicated code-reviewers** (one per bug class) + **adversarial verify per finding** + synthesis → 20 verified findings → **8 issues (0 HIGH, 6 MED, 2 LOW)**. Full detail in memory [[marley-hardening-sweep]].
- **Shipped (do-now cluster):** **M1** — relocked `communications`+`activities` READ `is_staff()`→`is_office()` (0024 missed them; a crew JWT could `GET` every quote/deposit email body + agreed_price). **Prod-RLS-proven** (rolled-back tx): crew reads 0/0 (was 30/79), office all. **M3** — HTML-escape customer-typed name/note/customer_name before internal ops-alert emails (new `lib/comms/escape-html.ts`; accept-flow + /s). **L1** — office-gate the 3 cubic share-link actions + `resendCertificateAction` (ran service-role, only `requireActiveProfile`). **L2** — `markLeadLostAction` single-winner CAS (no duplicate refund tasks/alerts). **M4** — try/catch/finally on 5 more await-behind-busy sites (card refund/mark-won×2/accept-quote/job-board save+drag). **M6** — 8s timeout on `videoDuration()` (AI-survey import can't hang at 0%).
- **Backlog M2 + M5 → NOW SHIPPED in `820744c`** (see the newer block above; ClickUp [869e6hwbj](https://app.clickup.com/t/869e6hwbj) closed): M2 = storage-billing delete-claim-on-failure + money alert; M5 = the 7 `fetchAllRows` reads.
- **NOTE — CI/CD is via GitHub Actions on push to master; during a GH-Actions incident the deploy job sits queued. Fallback = `bash scripts/deploy-ovh.sh` from i9 (add `--build-arg GIT_SHA=<sha>` for the version stamp — the raw script omits it).** 724 tests + tsc + lint + build green.
- Prior:

Last touched: 2026-07-19 on i9 — **ESTIMATOR INVOICING shipped (completion-basis commission) + hardened by 2-lens review; migrations 0064+0065 applied prod+dev, RLS-proven. Deploying via CI.**
- **Estimators now self-submit weekly invoices** like crew (reuses `staff_statements` anchored to the estimator's own staff row — Luke has a profile-linked active one). Three fixed-fee streams: **£50** per attended survey, **£10** per telephone quote (no visit, once per lead in the week first quoted), **7.5% of the ex-VAT job value ON COMPLETION** (Peter's decision — commission vests when the job completes, so a cancelled job never earns it and there's nothing to claw back from Luke). ex-VAT = gross/1.2 (the 20% customers are charged; **never** the FRS 1.10). New `/estimator/pay` list + editor + "My invoices" nav; office reviews/pays at **Finance → Contractor pay**. Full spec + deferred items in memory [[marley-estimator-invoicing]].
- **Retired** the Performance `estimator_payouts` mark-paid → reporting-only view pointing at Contractor pay (payment routes through the invoice, no double-tracking). Settings gained editable **phone-quote £** + **commission %** (admin-only). "Crew pay" nav/page renamed **Contractor pay** (covers crew + estimators).
- **Migration 0064** (line-source check += survey/phone_quote/commission; `estimator_phone_quote_fee`=10 + `estimator_commission_pct`=7.5 settings). **Migration 0065** (security).
- **2 opus reviews (money-correctness + RLS/separation-of-duties) → confirmed real defects, all fixed before deploy:** (a) **RLS self-pay/read-all-pay hole** — estimators are `is_office()`, so the `staff_statements`/`_lines` office branch (`is_office()`) let an estimator PostgREST-bypass every app gate (pay own invoice, set arbitrary total, read/tamper all crew pay, skip the signed-agreement gate). Fixed by 0065: office branch → `is_admin()` (the 0063 precedent); estimators fall through to the same signed-agreement, own-draft-only self-scope crew uses. **Prod-RLS-simulated: Luke (estimator, unsigned) reads 0 statements/0 lines, pays 0 rows, insert REJECTED; admin reads/manages all.** (b) **Commission cross-week double-pay** — a 2-day move straddling a Monday billed the 7.5% twice; fixed with a cross-statement ledger (`staff_statement_lines.lead_id` + per-appointment survey key) so each fee bills at most once ever. (c) owner resolution now skips null-estimator surveys (matches canonical); (d) crew `/my-jobs/pay` role-gated to `crew` so an estimator can't collide on the shared statement keyspace. Also separation of duties: mark-paid/void/return now **admin-only** (`requireAdmin`), office statements page redirects estimators.
- **Deferred (tracked, bounded after the fixes):** RLS MED-4 (a non-admin office user — i.e. an estimator — can still repoint `staff.profile_id` via the 0054 office branch; after 0065 this is a griefing/rate-leak, NOT a self-payment vector — needs a deliberate Staff & Fleet permission change, own commit); money LOWs (multi-accepted-quote value pick; a both-null-value accepted quote silently drops commission; VAT-registration-date floor ignored — all data-dependent/negligible); the rare phone-then-survey-later £10 over-pay (office review catches). ClickUp `AIOS Ops > Next`.
- **Go-live state:** self-billing is ON, but **Luke hasn't signed the contractor agreement** (Settings → Contractor agreement), so his invoice page shows the sign-first gate — nothing can pay until he signs + an admin approves; prod is still mock-data. tsc + lint(0 err) + **724 tests** + build green.
- Prior:

Last touched: 2026-07-19 on i9 — **CREW HOURLY-PAY MODEL shipped + hardened; Zoho test-invoices purged. Prod `86f68a8` (CI green); migrations 0061+0062 applied prod+dev.**
- **Crew pay reworked** (Connor+Leanne 19 Jul): flat day-rate → **hourly rate + optional weekly guarantee** (Rob £15/hr, £600/wk floor; Jack £15, Oscar £10, Charlie £13.50 — Charlie newly created; Crew Test £15). Crew invoice = hours × their **pre-filled own rate**; a guaranteed crew member gets a system 'guarantee' top-up line so a light/empty week reaches the floor (materialised at statement creation so Rob can submit a no-work week). **Rate privacy**: rates live in an office-scoped `staff_pay` table — a crew member reads only their own (for the prefill), can't see a colleague's or self-set (RLS-proven). Migration **0061** (staff_pay + 'guarantee' line source + null day_rate) + **0062** (one-guarantee-line partial unique index). Full design + IR35 flag in memory [[marley-crew-pay-model]].
- **Two opus code reviews** (money/RLS + stuck-UI) → every confirmed finding fixed in `86f68a8`: (a) Rob's empty guaranteed week now submittable; (b) no-rate crew blocked from posting £0 invoices; (c) concurrent-op duplicate-guarantee-line closed (0062 + line-button gating); (d) positive hours/rate required per line; (e) staff-save orphan-row rollback; (f) **root-caused Peter's mobile crew-availability FREEZE** — a class of `await`-a-server-action behind a busy flag with no try/finally (rejects on a flaky signal / stale post-deploy action-id → the control stays disabled+spinning, no revert, no toast). Fixed at 6 sites (availability normal-week chips + calendar cells, invoice runner + line save, note delete) + bounded a multipart-upload fetch. 709 tests + tsc + lint + build green.
- **Zoho cleardown**: 9 marley-ops TEST invoices (deposits + storage, £534) deleted from Connor's **LIVE** Zoho org — scoped by DB-recorded ids ∪ strict reference regex ∪ test-customer names; **0 real invoices touched** (receivables £40,226.80 → £39,692.80). Peter is setting up a **staging Zoho** so test invoices never hit the live books again.
- **OPEN — 2 decisions for Peter** (flagged, no code change): (1) a guaranteed crew member's expense/extra lines are absorbed into the £600 floor rather than paid on top — confirm intent; (2) **estimators (incl. Luke, himself a contractor) can read every crew rate** via `is_office()` on staff_pay — decide if that should be admin-only. Data cleanup: stale duplicate staff row **"Rob Pierce"** (unlinked, no rate) beside the real "Rob" — archive? And the standing IR35/employment ruling stays the go-live gate before the first real pay run.
- Prior:

Last touched: 2026-07-19 on i9 — **Go-live readiness review + technical hardening (bucket C). Prod on `5821c9a` (CI green).**
- **System-completion review (owner ask):** swept the go-live checklist + ClickUp + docs. The ops product is functionally complete; what's left is human inputs + cutover, not code. Aligned `docs/go-live-checklist.md` (ticked shipped items: staff availability, contractor invoicing; added **Section F — director due-diligence**: O-licence for the 7.5t truck + C1/CPC, waste-carrier reg for house clearances, ICO registration, insurance adequacy/Employers' Liability, IR35 check, accounting ownership + shareholders' agreement, H&S). New `docs/connor-go-live-agenda.md` = meeting-ready list (needs-from-Connor + confirm-in-place + accountant Qs).
- **Still-incomplete IN the system:** (a) built-but-off, needs an input — **card payments** (takepayments creds → env + switch), **lead auto-reply** (template sign-off → flip `LEAD_AUTOREPLY_ENABLED`), **fleet reminders** (`fleet_alert_recipients={}` → real Connor/Luke emails). (b) unbuilt month-1 — job-costing actuals, job-content publishing, client-merge UI, customer move-day confirmation comms.
- **Bucket-C hardening SHIPPED (`5821c9a`):** (1) **stale-bundle self-heal** — no Vercel Skew Protection on the OVH box, so `lib/chunk-reload.ts` auto-reloads a browser onto the fresh build on a ChunkLoadError (guarded 15s window), wired into both error boundaries; the UpdateBanner stays the proactive nudge. (2) **R2 photo size ceiling** — `MAX_IMAGE_UPLOAD_BYTES=30MB` on the survey-photo + crew-note buckets (server rejects over-cap + binds the presigned PUT; clients pre-filter). Video buckets were already capped. Deferred: R2 multipart/retry (TUS already covers video resumability). 704 tests + tsc + lint + build green.
- **AI-survey field validation** (iPad device sign-off + 30-survey shadow log, `docs/ai-survey-field-rollout.md`) is a HUMAN task, not code — flagged to Peter/Connor. Prior:

Last touched: 2026-07-18 on i9 (latest) — **CONTRACTOR INVOICING (renamed from self-billing) + one-time signed CONTRACTOR AGREEMENT shipped, feature SWITCHED ON. Prod on `07cf1a6` (CI green); migration 0059 applied to prod+dev; `self_billing_enabled=t` (LIVE).** Peter's asks this turn: rename to avoid HMRC "self-billing" confusion, draft a contract signed once per user in-portal + filed, and switch it on — all done.
- **Rename:** all user-facing "self-billing"/"payment statement" → **"Contractor invoicing"/"invoice"**. The crew PDF is now a **"Contractor Invoice"** (FROM the contractor / BILL TO Marley Moves — reframes it as the contractor's own invoice, sidestepping HMRC self-billing rules). NO-VAT invariant unchanged. Internal identifiers (`self_billing_enabled`, `isSelfBillingEnabled`, `StatementPdfData`) kept.
- **Contractor agreement (migration 0059):** versioned plain-English IR35-aware terms in `lib/contractor/agreement.ts` (`CONTRACTOR_AGREEMENT_VERSION="v1-2026-07-18"`, 10 clauses + 5 acknowledgments) + pure validators (8 tests). **One-time in-portal signature** (typed-name → script signature, the /s pattern): crew at **`/my-jobs/agreement`** (a GATE — can't build/submit invoices until signed, enforced in `meAsStaff` server-side AND the UI), estimators from **Settings › Contractor agreement**. Every signature **filed in `/documents` → new "Contractor agreements" tab**. `contractor_agreements` table: append-only, self-scoped RLS (sign as yourself, read own; office reads the register).
- **2-lens review (money + RLS/IR35), all findings fixed before deploy:** HIGH = the `office_write for all` policy let estimators (who are `is_office()`) forge/alter/delete a *peer's* signed agreement → **policy dropped** (office read already covers the register; append-only for all). Added an **acks CHECK constraint** so a direct-PostgREST insert with empty/partial acks can't create a "signed" row (proven: empty-acks insert rejected). Blocked signing by an inactive profile; swept rename leftovers. Browser-verified gate→sign→unlock→filed. 701 tests + tsc + lint + build green.
- **M1 CLOSED (migration 0060, prod+dev):** the invoicing gate is now enforced in **RLS**, not just the app. New security-definer `has_signed_contractor_agreement()` (mirrors `is_office()`) is appended to the crew branch of every `staff_statements`/`staff_statement_lines` write policy — a crew member can't create/edit/submit an invoice via direct PostgREST without a signed agreement. RLS gate = existence-of-any-signed-agreement (robust backstop that can't itself break invoicing); the app still enforces the precise current-version gate on top. **Prod-simulated: unsigned crew insert BLOCKED, signed crew insert SUCCEEDS**; browser-reverified the full signed-crew app flow (sign → create → add line) still works.
- **The agreement WORDING is a DRAFT** — recommend an accountant/solicitor review before crew rely on it; a reviewed v2 bumps `CONTRACTOR_AGREEMENT_VERSION` and everyone is re-prompted to sign. Prior:

Last touched: 2026-07-18 on i9 (later) — **PER-STAFF WORKING DAYS + CREW SELF-BILLING shipped to prod (self-billing DARK). Prod on `1a4e6e4` (CI green, `/api/version`=1a4e6e4); migrations 0055/0056/0057/0058 applied to prod+dev; `self_billing_enabled=f` verified.** Peter approved "ship all, self-bill dark" (availability + self-bill are bundled in one commit; self-bill rides along completely inert behind the OFF toggle).
- **Per-staff working days (migration 0055):** `staff.working_days int[]` default `{1,2,3,4,5}` (= today's Mon–Fri, so existing crew unchanged). Upgrades the 0053 availability model from a hardcoded Mon–Fri to a per-person baseline (a casual set to `{1,2}` no longer books Wed–Fri off every week). Threaded through `lib/staff/availability.ts` (pure) → crew portal, office wall chart, Job Board "N/N free" capacity. Crew mobile `/my-jobs/availability` rebuilt (normal-week chips + Timetastic-style month calendar + offer-a-weekend sheet); office **team wall chart** in Staff & Fleet (view + amend + live capacity).
- **Crew self-billing (migrations 0056+0057+0058, GATED OFF via `business_settings.self_billing_enabled`):** buyer-created **payment statement**, **NO VAT** (crew not VAT-registered — not a VAT self-bill; see [[marley-staff-self-billing]]). Crew `/my-jobs/pay`: start a Mon–Sun statement (0057 = one live per period), "Add my jobs" seeds day-rate lines from assignments, free-description manual lines, **"Use day rate £X" quick-fill chip**, submit = the crew's own generate+confirm (locks it). Refs `MMPnnn`. Office `/finance/statements` (admin/estimator; office-RLS): **expandable line detail + crew note** (no paying blind), **"£X to pay across N" wage-bill total**, Mark paid (BACS/cash/other) / Void, and **"Return to crew"** (0058 `return_reason`/`returned_at`: submitted→draft with a reason → crew get a "Changes requested" chip + amber banner, fix + re-submit). Return is the **IR35-safe amend** — office never silently rewrites a crew statement.
- Full round-trip **browser-verified on local dev** (submit → office expand/return → crew fix via day-rate chip → re-submit at corrected total → mark paid), dev cleaned after. tsc + lint + **693 tests** + build green; CI test gate + deploy + health-check all green.
- **OPEN — self-billing stays DARK until: (1) Peter's full walkthrough of the flow (requested this session), (2) a signed self-billing agreement drafted + accountant-checked (the IR35 go-live gate), then (3) flip Settings → Self-billing on.** Nothing pays anyone before that. Availability working-days is live now (low risk — defaults reproduce current behaviour). Prior:

Last touched: 2026-07-18 on i9 — **STAFF AVAILABILITY shipped end-to-end (crew mobile + office + Job Board) + a pre-existing staff-RLS hole closed.** Model (Peter): self-employed crew are counted as working **Mon–Fri by default**; a `staff_availability` row (migration **0053**, prod+dev) overrides one date — `unavailable` books a day off/holiday, `available` offers a weekend. No row = weekday available, weekend off, so login-less crew behave sanely. Pure logic in `lib/staff/availability.ts` (18 tests). **Crew self-serve** `/my-jobs/availability` (phone-first, optimistic one-tap `[Available | Off]`, entry card on /my-jobs). **Office editor** in Staff & Fleet → edit crew → *Availability* (single date or range → per-date rows; a holiday reads as one grouped "21–25 Jul" line via `groupAvailabilityRuns`; staff cards show upcoming chips). **Job Board payoff**: an unavailable crew member drops from the "N/N crew **free**" capacity strip exactly like an off-road van (generalised the cap row from van-only `offroad`→shared `unavailable`), shows an *Off/Weekend* reason, and warns (never blocks) on assign. RLS: office manages anyone (`is_office()`), crew only their own (`staff_id in (select id from staff where profile_id=auth.uid())`). **2-lens code review (correctness + RLS/security)** → 3 fixes (`fcd1a0d`): per-item optimistic revert (a whole-map revert clobbered a concurrent successful tap), forward-only office action (no orphan past-dated rows), `fetchAllRows` on the resources read (1000-row cap). **SECURITY (migration `0054`, prod+dev, RLS-simulated on prod):** 0020's `staff_update USING(is_staff())` had **no `with check`**, so any crew login could PATCH any `staff.profile_id` to themselves — hijacking the availability self-scope, reassigning Job Board identity, reading day_rates. Fixed: staff INSERT office-only; staff UPDATE constrained (crew can only update a row already linked to them or claim their **own email-matched** unlinked row on first login, via security-definer `my_email()`), never repoint profile_id. Crew login self-link **preserved** (sim: office update/insert OK, crew insert blocked, hijack blocked=0 rows, self-link + first-login claim both OK). **Prod on `df6371e`** (this session's full stack live). Also this session: landed Peter's stranded "ads validation verdict tile" growth commit onto master (cherry-pick `df6371e`, authorship preserved, backed up on `origin/wip/growth-ads-verdict` — deletable once he eyeballs /growth) and **reconciled his `marley-ops` worktree** (was ~88 commits divergent, stuck at the passkey-login era `cf7a3b7` → the "9 phantom lint errors" were stale files already fixed upstream; now in sync at df6371e, nothing lost). Lesson banked to `context/rules.md`. **OPEN — next build = STAFF SELF-BILLING** (discussed, not built): self-employed crew invoice us weekly; decided **internal build** (NOT Connor's Zoho — "our zoho is for us"; internal-basic OR a 3rd-party invoice tool), **no VAT** (crew aren't VAT-registered — so it's a buyer-generated *payment statement*, not a VAT self-bill; Invoice-Generator.com evaluated + **rejected**: its VAT-self-bill template/footer is wrong for non-registered crew + it's a renderer with no ledger/numbering we'd still have to build). Danger flagged: employment-status/IR35 optics — crew must **generate+confirm** their own statement, and a signed self-billing agreement (to draft + accountant-check) is a **go-live gate**. Deferred/lower: crew can still read `staff.day_rate` via direct API (`staff_read=is_staff()`) — UI never shows it; needs a column/view split. Connor+Luke real emails for fleet reminders still pending (session 35). Prior:

Last touched: 2026-07-17 on i9 — **TRAINING MATERIALS + CREW WALKTHROUGH VIDEO shipped.** Per-role illustrated in-app manuals + refreshed tours (`a4a9832`): role-rendered at `/manual` (admin switcher previews all three; crew manual at `/my-jobs/manual`, reading-age-9, drawn phone-frame SVGs, print-clean). Crew manual capture section corrected (`04df611`) — it wrongly pointed damage at the marketing camera; now two one-idea parts (9 total): **Part 5 "A problem? Write it down"** = private Crew notes & photos (`job_notes`/`job-photos`, office-only), **Part 6 "A good moment? Get it on camera"** = the `job_media` capture FAB WITH the ask-the-customer consent step. **These are two genuinely separate systems** — notes (`0028`, internal, never marketed) vs job-content (`0050`, consent-gated + office-approved at `/content` + hub-publish). **Crew walkthrough VIDEO now embedded** on `/my-jobs/manual` (`<video>` from `public/training/crew.mp4`; `.dockerignore` `training`→`/training` so the root pipeline stays out of the image but `public/training` ships). Video built by the standalone `training/` pipeline (Playwright real-UI footage + ElevenLabs "George" TTS + Remotion) — 3:12, retina-sharp (record at a 585×1266 viewport, NOT deviceScaleFactor), paced taps (~1s hold before each tap), 9 sections mirroring the manual incl. the consent card. Regenerable per release; source mp4 at `training/crew/out/`, review copy in Drive `/tmp`. Quality now gated by two new RBS-OS agents (`training-video-director` plans the shot list; `training-video-critic` vision-reviews the render) + the `role-training-materials` skill. Office + estimator videos are next (same pipeline, new scenario). Prior:

Last touched: 2026-07-16 (final) — closing polish: page-shell fix (/content + both scheduler pages had no padding wrapper; all 34 dashboard pages audited, convention captured above) and the go-live checklist gained the outstanding HUMAN items (A7 insurer policy docs, A8 tell Connor his login changed + close TEST claim CLM-001, B6 capture field pass on real phones, E publishing phase). Earlier — **CLAIMS STAGE 2 LIVE (master `97edc25`+`fa2d614`) + DAY-REVIEW HARDENING (`6bd96ef`)**: the incident-to-resolution register per the agreed spec — migration **0051** (dev+prod): `claims` table with CLM-nnn refs, status trail open→assessing→offer_made→settled/rejected/closed (DB check: terminal requires a resolution; goodwill requires the amount), sign-off auto-open idempotent via partial unique index on completion_id. Surfaces: **/claims** register (Customers nav, next to Documents) + **/claims/[id]** working page (status/resolution/insurer form with optimistic-concurrency stale-tab guard, print-clean insurer **evidence pack**: certificate PDF, contract, quote inventory, crew notes, captured content, 7-day T&Cs window chip), lead-page Claims card (shows on any completed job — even without a panel sign-off — with manual "Open a claim"), dashboard "Open claims" needs-action card, weekly-digest attention line. Every status/resolution/insurer change → lead timeline. **Live-E2E'd on prod: TEST claim CLM-001 (assessing) left on Reggie Fortune for Peter to inspect — close it as no_claim when done.** Stage-2 inputs still wanted from Connor: policy docs (insurer, excess, notification deadline). Then a **7-dimension adversarial review (46 agents) over ALL of today's ships** confirmed 38 findings — every one fixed same-night (`6bd96ef`, 4 parallel fix agents, 608 tests): capture = failed photo/voice uploads now retained with retry + visible failed state, save() survives signal drops with the tray intact, live 12-item cap, TUS abort on remove, no unattended recordings after the mic prompt, context-load error card + Retry; media server = transcribe cron reaps stranded 'running' rows + CAS claim, approve re-checks CURRENT lead consent, record verifies objects exist, x-upsert off, 24h review URLs; claims = concurrency guard, no trapped dialogs, £/comma amounts, chunked joins, pre-7am sign-offs task the SAME morning; digest/bank = **?to= preview now admin-only + crew blocked from digest/bank-feed routes** (new `requireOfficeOrCronSecret`), supersede deposits count once per lead, balance quotes fetched explicitly (no £0 weeks), pushes gate on the claimed row, >14-day-old transfers stop forming suggestions/paging, collapse digest reports the TOTAL pending. Known leftover (low): tus pause() aborts the in-flight request but true server-side TUS termination needs `lib/storage/tus-upload.ts` to expose abort(true). Earlier same day — **JOB CONTENT CAPTURE LIVE (master `617bd10`) + claims stage 1 (`ef3a637`)**: camera-first capture per PRD v1.0 (docs/job-content-capture-prd.md — pressure-tested first-hand after 4× API-529 blocked the research agents; two corrections: transcription got its OWN 5-min cron reusing analyseGeminiMedia because ai_jobs is constraint-locked to cubic surveys, and video uses the NATIVE camera input like photos with TUS-resumable upload — no custom PWA viewfinder). Capture sheet (full-screen one-thumb: Photo/Video/Voice, hold-to-record + slide-to-lock voice notes with runtime container detection, tag chips + caption optional, tray with progress/retry) on the crew job FAB + lead action bar "Capture"; job auto-attaches. Consent: leads.media_consent toggle + first-capture reminder; internal_only can never be approved (action + DB check). Office review: /content queue (Customers nav; needs-review/approved/internal tabs) + lead-page "Job content" card — **only office-approved items ever reach marketing (Peter approves initially)**. Migration 0050 prod+dev (job_media + private job-media bucket + leads.media_consent); transcribe cron in cron.d + registry, first run seeded ok. 597 tests. **Publishing phase (Drive hub sync → 08 Media Library/real/jobs/approved → existing agents) designed in PRD §9, NOT built — next conversation.** Field pass pending on real phones (PRD §7H checklist: standalone-mode mic permission, HEIC, big video on 4G). Claims stage 1: sign-off exceptions raise a next-morning call task (source sign_off_exception, deduped) + office alert — an exception can no longer die silently; stage 2 (claim record + insurer pack) is month-1 pending Connor's policy docs. Earlier: **WEEKLY OWNER DIGEST LIVE (master `7b9f9b5`)**: Monday 06:00 UTC email to the active on-domain admins (Connor + Peter) — money in, enquiries, quotes sent, won/lost + values, jobs done, surveys, each vs the prior week with trend arrows; biggest win (first name only), next-7-days booked moves/surveys, storage occupancy, and an attention block only when non-zero (sign-off exceptions, unmatched bank transfers, open follow-ups). Weeks = UK Mon–Sun, always the COMPLETED week. Pure engine lib/digest/weekly.ts (591 tests); cron.d line added on the VPS; first fire seeded cron_runs (watchdog-safe) with a preview to the sink (`?to=` param). First real send Mon 20 Jul — will reflect TEST data until cutover. **Decisions this session: fresh-start cutover (no historical backfill — only open quotes/booked jobs/storage lets hand-carried; kills the chase-safety-at-backfill risk); keep marley-ops as the CRM, Zoho stays ledger-only; B2B client support = build-ready module for when business accounts arrive (client kind, ex-VAT presentation, terms/PO instead of deposit-first, B2B chase tone — no business customers today); staff holidays deferred → TimeTastic integration when permanent staff exist.** Earlier: **EMAIL IDENTITY & ROUTING LIVE (master `297814c`)**: per docs/email-identity-plan.md (Peter approved same day; all six IONOS mailboxes confirmed; accounts@/peter@ creds in memory `marley-mailboxes.md` for future bookkeeping). Quotes/chases/deposit requests/survey confirmations/review requests/composes send From the lead OWNER's own mailbox ("Luke at Marley Moves <luke@marleymoves.co.uk>"; off-domain/inactive/crew → hello@); money emails (receipts, balance invoice, storage bills — quotes@ hardcode fixed, card refunds) From "Marley Moves Accounts <accounts@>"; ops alerts routed money→accounts@ / system→peter@marleymoves.co.uk / business→hello@ (env set on VPS: OPS_ALERT_EMAIL=hello@, _MONEY=accounts@, _SYSTEM=peter@); inbound replies forward to the LEAD OWNER only (Peter's pick — panel follow-ups are the shared net; INBOUND_FORWARD_EMAIL=hello@ is now just the fallback) and unmatched inbound mail is no longer silently dropped (forwards to hello@ behind a loop guard). **Logins: Connor swapped to connor@marleymoves.co.uk (same password + passkey — TELL CONNOR his sign-in email changed); stale luke@marleymoves.test deactivated; Luke/Peter already real.** 27-agent adversarial review same-day: headline fix = ONE canonical owner rule (`leadOwnerIdentity`: leads.estimator_id → survey-derived → quote creator) for outbound From AND reply forwards (the webhook originally used the quote CREATOR — Luke's threads would have forwarded to Connor after the login swap); plus HTML-escaping in forwards, From-header hardening, crew role guard, local-part-anchored robot guard, compose replies → composer's mailbox, refund replies → accounts@. 580 tests. Fallback chase signature de-Peter'd (team-signed). Live proofs pending: chase 2 (~19 Jul) fronts the owner identity unattended; one real inbound reply. moves@→hello@ alias = manual IONOS step. Earlier: **BANK-PAYMENT ADMIN PUSHES (master `0082fae`, live)**: the 2-min bank-feed sync now pages ADMINS ONLY (Peter + Connor — estimators keep the post-confirm "Deposit received" push) the moment a transfer is surfaced: matched → "Looks like Jane's deposit (MMR001) — one tap to confirm" deep-linked to /payments; unmatched/mismatch → "needs a look/matching". Transition-gated (info→suggested/unmatched, unmatched→suggested) so re-passes never re-ping and the deploy was storm-free (live pass: 208 pre-surfaced rows, `notified:0`); >3/pass collapse to a digest; one OS tag per transfer so a later match REPLACES the stale alert; `payment_event` kill switch + per-user prefs apply; /payments added to the deep-link allowlist (payload.ts + sw.js lockstep); firstNameOnly de-shouts ALL-CAPS statement names. Confirm still runs the FULL paid pipeline (Zoho record → lead confirmed/settled → chase closed → timeline → office push → branded customer email) — deliberately never auto-confirmed. 570 tests. **Live proof pending: Peter's £1 self-transfer** (push should land on both admin phones within ~2 min; dismiss on /payments after). Earlier: **PRODUCTION-HARDENING SWEEP (master `c1a5cc1`)**: backup RESTORE drill PASSED (02:30 dump → scratch DB, counts reconcile; dump's BOM/CRLF tolerated by psql — leave pipeline alone); **external uptime monitor LIVE** (i9 task "AIOS Marley Uptime", 5-min /api/version ping → WebEx SMS after 2 fails + recovery SMS; script `O:\RBS-OS\scripts\marley-ops-uptime-monitor.ps1`); **VPS disk watchdog** (hourly, SMS at 85%, now 32%); **error boundaries** (app/error.tsx + global-error.tsx — branded recover screens with digest); **S5 chase suite RUNNING** (chase 1 fired live at natural d2 for MM-260714-002 → sink; chase 2 self-fires ~19 Jul, chase 3 ~24 Jul; NOTE: Leanne's real-email test lead got a real deposit chase 15 Jul — team test leads with real emails DO get chased). Checklist B3/B4 ☑. Earlier: **BANK FEED HARDENED per 16-agent depth review (master `ba4464d`) + HEALTH WATCHDOG live (`df0093b`)**. Review: 13/13 confirmed, 0 refuted. CRITICAL fixed: suggestions now REQUIRE the exact amount — wrong-amount reference matches become non-confirmable "mismatch" rows (new /payments card "Transfers that need a human"); confirm is claim-first CAS bound to the exact (quote,kind) the office saw; `already` propagates → duplicates surface loudly. Mediums: per-item first-wins dedupe, status-guarded matcher writes, UNFORMATTED_VALUE + skippedRows counter, poison-date round-trip, chunked+throwing lead lookup, 35-day upsert window (writes 1,887→346/pass, confirmed/dismissed rows never rewritten). Lows: migration **0049** (estimators see non-info rows only; full statement admin-only), Zoho truncation flag on Outstanding, FRS rounding note. **Watchdog**: /api/cron/health-watchdog every 15 min — overdue-automation + feed-staleness alerts SMS Peter (OPS_ALERT_SMS via WebEx, 6h cooldown, ?smstest=1 proven live); external /api/version monitor still needed for total-outage (checklist B4). Earlier: **MONZO BANK FEED LIVE (master `c6477cb`)**: /payments' seam is real — 2-min cron reads the Monzo→Sheets export (sheet `1wkuyQiKiV…`, tab "Business Account Transactions", narrow spreadsheets.readonly token GOOGLE_REFRESH_TOKEN_MARLEY_BANKFEED — never the broad RBS token), upserts ~1,900 rows into bank_transactions (migration 0048 prod+dev), matches inbound FPS/BACS/**Monzo-to-Monzo** (live £1 test taught us that type) against awaiting deposits/balances by quote ref; suggestions confirm-to-record via the existing paid pipeline, never auto-marked. First live sync caught a REAL £100 deposit (Andrew Scott, ref MM-260709-104, unmatched — its quote predates the panel). 208 historical inbound tracked. TWO live-caught fixes: Monzo-to-Monzo classifier + the PostgREST 1,000-row cap (fetchAllRows — the session-30 lesson bit again in new code). **Dev-DB drift healed**: local dev was missing migrations 0035–0042 (checkatrade enum, review_suppressed, passkeys, push) — applied + Supabase types regenerated. Also today: **FLAT RATE SCHEME wired in (master `0b101e5`, live)**: Marley is on the FRS at 10% (removals/transport rate — Peter). Invoices still charge 20%; Finance's day/month/quarter figures now show what's OWED (10% × gross post-registration turnover, computed on the period total like HMRC) with the 20% charged alongside — live: today owed £297.92 vs charged £496.53; MTD owed £1,565.91 on £15,659.11. Scheme + % are Settings controls (migration **0047** prod+dev, prod row flat_rate/10); ASK ACCOUNTANT: first-year discount (9% until 31 May 2027?) + invoice-vs-cash turnover method. Earlier: **FINANCE group SHIPPED (master `a179b07`, live, verified against the real books).** New nav group Finance = Payments (moved from Sales) + **/finance "Invoices & VAT"** (admin-only — Zoho bypasses RLS so the page gates by role): invoices raised per UK day straight from Zoho (hand-raised ones included — it reports the BUSINESS, not the panel), per-invoice net/VAT/gross at 20% inclusive (gross÷6, per-invoice rounding matching the documents), output-VAT day total, month-to-date, all-dates outstanding, Zoho deep links, fail-soft banner. Engine `lib/finance/invoices.ts` (13 tests; 522 total). Live-verified: real invoice INV-000156 (Freddie Griffin £2,979.15 = £2,482.63 + £496.53 VAT) on the day view; outstanding £33,089.80 across the org. Marketing group relabelled **"Marketing (Growth)"** (fed by the growth engine's artifact push). **VAT quarter-to-date card SHIPPED (`5419f5e`)** + **VAT registration floor (`1615302`)**: the HMRC approval letter (VRT22C, 16 Jul) confirms VAT no. 520 2213 58 **registered with effect from 01 Jun 2026** — pre-registration invoices now carry £0 VAT on /finance (live-proven: May 2026 holds £11,606.58 of Zoho invoices, quarter VAT correctly £0 when browsing then). The letter does NOT state the stagger — Connor gets it from the Business Tax Account (first return period end) or his accountant/agent, then Settings → VAT quarter cycle (default Mar/Jun/Sep/Dec). Input VAT stays in Zoho/accountant. Earlier (15 Jul): **master `602b3df`** — Final fixes of the day (Peter's field reports): quote-wizard **Days on the job now uses the shared 44px +/- Stepper** (was a bare number input — iPads show no native spinners); **"New quote" for an existing client** — client page action bar gained "New quote" → `/quotes/new?clientId=` and the form pre-selects that customer (combobox + contact seeded via `initialClientId` on AddLeadForm; the /quotes/new combobox itself already worked, it just wasn't reachable from the client). Both prod-verified (stepper 1→2→1 clamped; Harriet Vane prefill; test draft MMR003 deleted — its manual-enquiry lead remains on the seeded client, leads are never deleted). Earlier: **Nav regroup + workflow guards SHIPPED then HARDENED via 15-agent depth review (`00e782b`, prod-verified).** Review confirmed 8 defects, all fixed same-night: week deep links now use the Europe/London day not the UTC slice (all-day Monday moves during BST linked to the previous week — HIGH); board/status-select reseed from server props + handle the server's needsReason verdict by opening the reason dialog (stale-tab dead-end closed); thrown actions revert optimistic state with a toast; Job Board follows ?week= when already mounted; updateLeadStatusAction write is conditional on the read status (TOCTOU); MarkLostDialog can't close mid-unwind; reason dialogs protect typed text from backdrop taps; ?week years bounded 2000–2100. Office nav now: Pipeline (Dashboard/Leads/Follow-ups/Pipeline Board) · Sales (Quotes/Bookings/Payments) · Schedule (+Completed Jobs) · **Customers** (Clients/Documents) · Operations · Reports · **Marketing** (Website & Tracking / Ads — Growth renamed) · System; page eyebrows + manual aligned, tours unaffected (href anchors). Deposit-paid toast now offers the next step (Assign crew → Job Board at the move's week via new `?week=` deep link, or Book removal); Booked rows carry a Job Board link. **Backward funnel moves require a reason** (`isBackwardMove` in lib/leads/funnel.ts, server-enforced in updateLeadStatusAction, reason on the timeline — prod-verified round trip) and **"Declined" can no longer bypass mark-lost** (status dropdown + kanban both route to the MarkLostDialog; raw declined writes rejected server-side). 509 tests. Same session: **go-live test plan** (`docs/go-live-test-plan.md`: S0–S15 suites, cutover runbook with the chase-safety-at-backfill gate, sign-off criteria). Earlier same day — **CARD PAYMENTS (takepayments) SHIPPED + full UI colour pass, master `16ecd0c`, live.** Colour pass (Peter disliked the muddy pink highlight; TWO sweeps): `mm-red-tint` token → rose-50 `#fff1f2` and the tint RETIRED from every control/badge — selected chips/toggles/tabs are solid mm-red + white system-wide; status pills moved to semantic families (No-answer=danger, repeat-client=success, Quoted/Sent/Contract=red outline, Move/occupied/stat pills=solid red, Not-moving=warn, freshness ramps start neutral); composite option cards (quote wizard, AI review rooms) use red-outline selected style; today markers = red rail + solid red date pill (Job Board) / solid red tile (/my-jobs); Job Board colour-coded (traffic-light capacity, real status colours, red Move rail vs teal Survey cards, green-met/red-short crew line; `survey-deep` token). Remaining tint = hovers/soft callouts only. **Dialogs close on backdrop click** (standard behaviour) via `ignorePortalInteractOutside` — portalled-Select clicks still don't phantom-close; email compose never drops a typed draft. All browser-verified on prod (backdrop-close tested live). PRD `docs/takepayments-card-payments-prd.md` → build → 3-agent adversarial review (all findings fixed): /q "Pay by card" → hosted payment page (deposit only, no stored cards, no recurring), signed callback settles via the existing deposit-paid pipeline, admin refund/void with full UI lockout on top of the server-side atomic reserve, Settings kill switch (**OFF**) + test button. Migrations **0043–0045 applied to BOTH prod (verified + pgrst reload) and local dev**. `cron/card-reconcile` added to `/etc/cron.d/marley-ops` (5-59/15, fired once: `{ok:true}`). **DARK until Connor's merchant creds** → `TAKEPAYMENTS_*` env (app.env + .env.local) → flip the switch → Settings test payment. Same day: **/payments** day view (Sales nav, office + estimator — card receipts/refunds + panel-recorded deposits/balances, UK-day windows, bank-feed seam; engine `lib/payments/received.ts`, 506 tests total); **tour fires once per account** (`profiles.tour_seen_at`, proven live) with the relaunch as a small TOUR button beside the version stamp on every surface; estimators got Settings in nav (page trims to Quick sign-in + Notifications); crew /my-jobs device rows grouped under "Your device" — all role views browser-verified on prod. External push test to Connor's iPhone accepted (201); Connor enrolled device + passkey; Jack/Rob/Luke not enrolled yet. **Bank-feed decision PENDING Peter** (Sheets feed = Monzo Business Pro only £9/mo; Starling £0 API alternative; Revolut Basic has no path — memory `marley-bank-feed.md`). ClickUp: Next 869e58b5v/5x/5y.

Earlier: 2026-07-13 (evening) — **Prod is in OPS-VALIDATION mode.** Data wiped + reseeded via `scripts/reset-data.mjs` + `scripts/seed-ops-validation.mjs` (run inside the app container): a lead at every pipeline stage, every contact = peter@abacusonline.net / 07572382366. `COMMS_DRYRUN=false` — sends are REAL but only reach Peter's sink + the real staff mailboxes. Prod users added: luke@marleymoves.co.uk (estimator), jack@/rob@marleymoves.co.uk (crew), staff rows linked. Old `.test` users + unlinked staff rows (Oscar, Rob Pierce) still present. [PR #7](https://github.com/redbananastudios/marley-ops/pull/7) (Run-now automation buttons + the reset/seed scripts) awaiting merge. Marketing-side validation comes after ops validation passes.

Earlier: 2026-07-13 on i9 — **Growth section MERGED + LIVE on prod** ([PR #6](https://github.com/redbananastudios/marley-ops/pull/6) merged, CI deploy green). Office-only nav group: `/growth` (launch readiness — verdict, tracking gaps card with per-platform missing events, leads-by-variant on `utm_content`, artifact freshness) and `/growth/ads` (proposal-only creative matrix, ChatGPT Ads brief, optimizer recs). Migration `0034` (`growth_artifacts`) applied to BOTH local dev and prod Supabase; 10 artifacts in each. Delivery: `O:\RBS-OS\agents\tools\growth_push_ops.py` (PostgREST for dev; `--emit-sql` piped over SSH+psql for prod, so the service key never leaves the VPS), refreshed nightly by i9 task `AIOS Growth Ops Push` (07:00). Tracking validation runs for real against PostHog project 202362: status **fail** — GA4 fires 5/9 critical events (all missing `variant_key`), PostHog 0/9 canonical. Launch stays blocked until the site fires the spec events and validation passes. 293/293 tests.

Earlier same day — **Marley Ops fully migrated off Vercel + shared vps1 onto a dedicated OVH VPS**, with GitHub CI/CD. Migration complete and end-to-end verified.

**Where it runs now (all on the OVH VPS `51.195.253.165` / `vps-a0b9c066`):**
- **App** `ops.marleymoves.co.uk` — Next.js standalone Docker container (`marley-ops-app`), fronted by **Caddy** (auto Let's Encrypt TLS).
- **Backend** `supabase.redbananastudios.com` — the full self-hosted Supabase stack (11 services), a byte-faithful clone of the old vps1 DB (data hashes verified equal), same JWT/anon/service keys so no session breakage.
- **Cron** — 7 jobs via on-box `/etc/cron.d/marley-ops` (fires every 2 min, verified unattended). Replaces Vercel Cron.
- **Env** — `/opt/marley-ops/app.env` (54 vars, assembled from Vercel + credentials.env).
- Both IONOS DNS records A → the box (TTL 60). All containers `restart: unless-stopped`.

**CI/CD (new):** push to `master` → `.github/workflows/deploy.yml` runs the test gate (lint/tsc/278 tests) on a GitHub-hosted runner, then a **self-hosted runner on the OVH box** builds + restarts the app + health-checks. Runner = systemd service `actions.runner.redbananastudios-marley-ops.ovh-vps`. Manual fallback: `bash scripts/deploy-ovh.sh` from i9.

**Full ops + rollback runbook: `docs/ovh-deployment.md`.** Covers VPS access (`ubuntu@51.195.253.165`, key `~/.ssh/rbs_vps`, key-only SSH + UFW), deploy, DB migrations (SSH → `docker exec supabase-db psql` + pgrst reload), env changes, nightly backup (`scripts/backup-prod-db.ps1` → OVH, verified), and rollback.

**Vercel: DELETED** and **vps1 Supabase: TORN DOWN** (both 2026-07-13, per Peter — the OVH box is now the sole live copy; Red Taxi on vps1 untouched). Final vps1 snapshot + on-disk data kept as safety nets (`backups/marley-ops-vps1-final-*.dump`; vps1 `/opt/rbs/supabase/volumes`). All testing happens on the new VPS; real-lead backfill (SANITY_SYNC_DISABLED) is a later go-live step. New sudo password for the box recorded with Peter (break-glass; SSH is key-only).

**Prior state (still current):** PR #5 (premium role UI + /automations + hardening) is merged + live; migration `0033` (`cron_runs`) applied. **Go-live checklist still open:** generic terms legal review (ClickUp 869e35z42), SANITY_SYNC_DISABLED removal + lead backfill, `.test` team emails → real, INBOUND_FORWARD_EMAIL/OPS_ALERT_EMAIL → office address, Stripe card button, iMVE cutover decision, deferred audit mediums (ClickUp 869e378hj). No blockers on the code — green, deployed, self-hosted.
