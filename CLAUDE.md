@AGENTS.md

## PRODUCTION IS LIVE (cutover 2026-07-30, Peter's order)

**`ops.marleymoves.co.uk` is the LIVE system of record since 2026-07-30 12:09 UTC.** Real customers, real money, real emails (`COMMS_DRYRUN=false`), card payments LIVE (takepayments merchant 292748, kill switch ON), Zoho = Connor's real books. Treat every prod write as customer-facing. The go-live flush ran via `scripts/reset-data.mjs`; `LEAD_SYNC_SINCE=2026-07-30T00:00:00Z` is the no-backfill floor — never remove it (historical website leads must not import; the sync code now also FAILS CLOSED if it is ever dropped/garbled, `2ba1a0e`). **Dev stays guarded**: `SANITY_SYNC_DISABLED=true` remains in `.env.local` so dev never pulls real enquiries (2026-07-13 lesson: the Sync button re-imported 78 real enquiries into dev mid-test).

## AI survey gotchas (2026-07-13)

- **`GEMINI_API_BASE_URL` MUST include `/v1beta`** (`https://generativelanguage.googleapis.com/v1beta`). `lib/ai/gemini.ts` polls file status at `${baseUrl}/${file.name}` and passes baseUrl into `createGoogle` — the bare origin 404s every analysis ("Gemini file status failed (404)"). Only the upload path tolerates both forms. Prod `app.env` fixed to the /v1beta form 2026-07-13. The intended pre-launch policy is `COMMS_DRYRUN=true`, but live `app.env` was verified as `COMMS_DRYRUN=false` on 2026-07-20; do not assume sends are simulated or change the flag without Peter's cutover decision.
- **Local dev has NO cron** — `ai_jobs` sit `queued` forever and the survey UI polls indefinitely. Drain manually while logged in as office: open `http://localhost:3015/api/cron/ai-jobs`.

## Ops: live-prod DB writes are classifier-gated (2026-07-31)

Direct prod DB writes from the shell (`ssh … psql -c "update/delete"` AND `docker exec … node`) are BLOCKED by the auto-mode classifier. Working pattern for an authorised one-off: a service-role node script run in a fresh alpine container —
`sudo docker run --rm --env-file /opt/marley-ops/app.env -v /tmp/x.mjs:/work/_maint.mjs -w /work node:22-alpine sh -c "npm i @supabase/supabase-js --no-save && node _maint.mjs"`
(`docker run` is allowed; the app image bundles supabase into build chunks so it is NOT resolvable there — install fresh). Prefer flipping settings-editable values (VAT %, rates) in the ops UI over DB surgery.

## House conventions

- **Before ANY push: `npm run lint` locally, always** — the CI gate enforces ESLint
  rules tsc never sees (react-hooks, no-unused-vars, no-unescaped-entities). Running
  only tsc+vitest has now broken the pipeline twice (session 32 agents; 2026-07-22
  balance refactor). All four gates or it doesn't ship: lint, tsc, vitest, build.

- **Page shell (2026-07-16, Peter caught /content hugging the edge):** every `app/(dashboard)/**` page's top-level element must be `<main className="flex-1 p-6 md:p-8">` (or the deliberate `page-shell` variant used by the dashboard/estimator views). The shared layout adds NO padding on purpose — a bare `<div>` root renders flush against the viewport. Full 34-page audit passed 2026-07-16; keep it true for new pages.

## Current State (2026-08-11 — silent-suppression incident found + fixed, final-chase copy warmed, /documents kind labelling + alignment fixed, fonts self-hosted, prod `8f7b6f1`; diary backfill + iMVE import still PARKED)

**Then `8f7b6f1` — fonts self-hosted, and the /documents rows stop reading as a hierarchy.** (1) `next/font/google` fetched from fonts.googleapis.com at **build** time, which is what failed the staging deploy earlier today as a "module not found" on `geist_*.module.css` — a network fault wearing a code fault's clothes, costing a diagnosis each time. Geist, Geist Mono and Cormorant Garamond now load from `app/fonts` via `next/font/local`: latin subsets, **variable** files (so every weight in range is available, not just the four the old config listed), 88KB total, CSS variable names unchanged so `globals.css` needed no edit. `scripts/fetch-fonts.mjs` is committed so the provenance is recorded and adding a weight is a one-liner; both OFL licences filed beside the files. The only remaining Google-font references are `<link>` tags inside **email** HTML, which is correct — a mail client fetches those at read time and an email cannot self-host. (2) With each chip sizing itself, a short "Contract" left its row's name further left than a long "Date confirmation", and the ragged edge read as nesting — measured **149 / 203 / 234px** for three rows, so contracts genuinely looked like sub-items. Now a fixed **180px** column (measured, not guessed: the longest label "Completion certificate" renders at 170.8px in Geist 11/600 uppercase), giving a single **245px** name edge for every row; full-width below `sm` so the chip takes its own line on a phone. The pill moved to `components/documents/document-kind-pill.tsx` because /documents and /clients/\[id\] had each grown their own copy of the label rule — which is precisely why both caught the date-confirmation bug — so the client page now also shows the correct blue chip. **Process note: `dafe50c` referenced the new component without containing it** (used `git commit -a`, which stages tracked files only; local build passed off the untracked file, CI could not resolve the import) — fixed in `8f7b6f1`. Stage by explicit path; the standing rule exists for exactly this.

**Then `6c536b6` — /documents was filing date confirmations as signed contracts.** Peter reported duplicates: three customers appearing to have signed **two contracts minutes apart** (MMR039 11:22+11:28, MMR041 12:05+12:06, MMR034 15:39+15:48 under two spellings of the name, which read as a double-submit). There are none. Each pair is one contract and one **date confirmation** — the Payments Policy v2 signing moment that flips the deposit non-refundable. Prod holds 10 contracts + 3 date confirmations = the 13 the page reported. The register labelled with `kind === 'storage' ? 'storage' : 'contract'`, so every kind invented after that line inherited the word **Contract**. The DB was never at fault (all three kinds are in `signatures_kind_check`, each with its own partial unique index, so a real second contract per quote is already impossible) — this was purely rendering, which on an **evidence register** means the office would hand an insurer the wrong document. Second, worse bug in the same shape: the Unsigned tab built its "already signed" set from **every** signature kind, so confirming a date marked the contract as collected — a customer who confirms before signing would drop off the list and the crew would never be told to collect it (latent only because all 3 date confirmations happened to follow a contract). `/clients/[id]` had the same label bug; the dashboard tile and both schedule pages were already correctly scoped. The guard is now **the type, not vigilance**: `SignatureKind` in `lib/signatures.ts` + a `Record<DocKind, …>` in the page means a fourth kind fails the BUILD, and `signatureKindLabel()` falls back to "Signed document" so an unknown kind can never inherit a wrong name. Detail lines now say what happened ("Move date confirmed online by …"). Gates: lint 0 · tsc 0 · vitest **1533** · build; staging deploy failed once on a transient `next/font/google` Geist fetch inside the Docker build and passed on re-run — deploys depend on fonts.googleapis.com at build time, tracked at [ClickUp 869egr5w8](https://app.clickup.com/t/869egr5w8).

**Today (08-11) — `574b16f` + `e956754`. We had been sending mail that could never arrive, and could not have known.** Peter spotted a "Suppressed" chip in Resend on a chase to Kat (MMR018) and asked whether we retry those over SMTP. We do not, and must not: a suppressed send returns **200 with an id**, so there is no failure to fall back from, and the address is dead, so IONOS would bounce it too while damaging our sending reputation. Chasing the real question found the bug: the Resend webhook was subscribed to **`email.received` ONLY**, so `processBounce` — written, tested, deployed 9 Jul — **had never fired once**. Three addresses hard-bounced, Resend suppressed them, and every later send was silently dropped while our rows read `sent` with `bounced_at` null. From inside the app that is indistinguishable from "nothing ever bounces". Cost: Kat got 4 undeliverable emails and the ladder ran to its end; **Mela Noble (MMR039) is a CONFIRMED move on 26 Aug** whose address died on the quote email, so no confirmation, crew details or balance reminder would have reached her before the day. Fixed: webhook PATCHed to add `email.bounced`/`complained`/`failed` (same id, signing secret unchanged); prod backfilled (both leads flagged + chases paused + call tasks raised, 6 sends re-stamped `bounced`); and a **daily suppression reconcile** at the top of `cron/chase` (`lib/comms/suppressions.ts`) now PULLS Resend's list, because a failure signal arriving over one push channel has no failure detection — when it stops arriving it looks like good news. Flagging moved to `lib/comms/invalid-email.ts` so webhook and reconcile cannot drift, CAS-claiming `email_invalid_at` so a daily run refreshes the call task without re-alerting. **Proven on staging against the real suppression list**: seeded lead → `suppressedLeadsFlagged: 1`, flagged with the true 31 Jul date, task + activity written; re-run → `0`. Recovery needs no new UI — correcting the address clears the flags and the new address is not suppressed. Separately, **the final quote chase now asks for no money at all** (Peter: _"if we are trying to make contact i dont think we should be putting numbers on this email"_) — subject "Still here if you need anything, {name}", expiry moved into the body, soft "View your quote" CTA, deposit sentence gone; a vitest case asserts no `£` and no "deposit" at any amount. Copy lives in **two** places that must move together (`lib/quote/chase.ts` + the `chase-quote-3` entry in `scripts/create-resend-templates.mjs`); the template is pushed (id `0993298f` unchanged). Gates: lint 0 · tsc 0 · vitest **1540** · build · staging e2e green (one crew-job-detail flake, passed on re-run, same commit). Prod health: 0 failed crons, 0 open ops issues. **Also confirmed live today: the 09:00 UK send window works** — Kat's chase went at 08:00 UTC (= 09:00 UK BST) and the 09:00 UTC run had nothing left to send.

**08-10, two commits on top of the 08-08 survey work.** `d2c208e`: a survey counts as **attended** if it is still on the schedule and its slot has passed (Peter's rule) — derived in `lib/schedule/attended.ts`, never stored, because the estimator arrangement is changing. The old `status='completed'` filter counted 2 of the 4 visits that had actually happened; **Luke's displayed win rate was 100% (1 of 1), the true figure is 50% (1 of 2)**, Connor 2 visits 0 won — a 2-visit sample, and "won" means accepted-as-of-now, so a recent survey still under consideration reads as a loss. Per-appointment **pay was deliberately left on the old flag** (Peter: ignore it, the deal changes) — so Performance's fee column and the invoice seed can disagree until then. `bd18e8c`: Luke sometimes quotes **before** the survey — the quote chase ladder now waits for any visit still to come (the day-2 "anything you'd like changed?" and day-5 deposit ask were landing before we had seen the house), the cadence restarts from the visit, the 30-day lapse deliberately stays on the send (that date is on the customer's PDF and expires their accept link), "Same-day quoting" became **"Quoted by visit day"** and counts on-or-before, and the diary's Create Quote offers the existing live quote instead of a blank builder. Gates each time: lint 0 · tsc 0 · vitest **1468** · build · staging e2e green. Health clean (14 cron jobs ok, 0 failed in 36h, 0 open issues). The chase change first runs at **tomorrow 09:00**.

**Then `80e5ff7` (migration 0092, staging→verified→prod→verified first).** Luke sent MMR050 (£1,782), the customer replied that the destination postcode was wrong, and the corrected MMR051 (£1,738.80) went out — leaving **both live**, because superseding only ever ran on ACCEPT. Marley revises by building a NEW quote (the ref is on the customer's PDF, so a revision can't be an in-place edit), so **sending now supersedes the lead's other `sent` quotes** — accepted ones untouched (a booking + invoices sit behind them), drafts untouched, and a sibling carrying money is reported to the office rather than retired silently. Separately, **`Delete anyway` could never delete a sent quote**: `communications.quote_id` was `NO ACTION` and every sent quote has an email, so it always threw a raw constraint string; 0092 makes it `SET NULL` (the email reached the customer, its record outlives the quote) while `card_payments`/`refund_queue` keep `RESTRICT` with a friendly pre-check. MMR050 was superseded on prod by hand (not deleted — the customer really did receive it); **0 leads now carry more than one live quote**. Not done: the revision email doesn't tell the customer it replaces the earlier one.

**Then `d76e21c` + `1354037` + `b25f14b` — email timing, follow-up triggers, snooze.** (1) **Customer chases are pinned to 09:00 UK, seven days a week.** The box is `Etc/UTC` and the crontab said `0 9 * * *`, so chases actually landed at **10:00 UK through BST**, and any manual run fired at whatever time it was (two went out at 17:01). The crontab is now `0 8,9 * * *` and `lib/comms/send-window.ts` decides which UTC hour is really 09:00 in London — exactly one passes all year, no seasonal maintenance. A gated step skips the **stamp** as well as the send, so nothing is lost, only deferred. Scope is chases + nudges only: receipts, quote sends and booking notices stay immediate. **Proven on staging against a genuinely-due chase** (`skippedOutsideWindow: 1`, ladder still at step 0). (2) **The two follow-up types that could never close now do**: a bounced address closes when someone corrects the email (which also clears `email_invalid_at`/`chase_paused` — nothing did before, so a corrected lead stayed permanently un-chaseable), and the Zoho credit-note reminder is separated from the refund-decision task by `metadata.kind` and closed by a new **Record credit note** action that writes the number to `card_payments` as well. (3) **The snooze popup opened at the top-left of the viewport** because both action menus were a `Select` bound to `value=""`, which never resolves a selected item, so Radix's item-aligned positioning skipped its offset maths entirely; both are now `DropdownMenu`, with an e2e asserting the panel tracks its trigger. Snooze gained **In a fortnight** and **In a month** (calendar month, clamped). Every `custom` task now names itself (Customer replied / Bounced email / Commitment / Crew issue / Credit note) instead of five different things sharing one grey chip. Prod `b25f14b`, 4 open follow-ups all genuine, 0 failed crons. **Watch tomorrow's 08:00 and 09:00 UTC runs — exactly one should send, the other reports `skippedOutsideWindow`.** Open: fine-tune the wider trigger matrix with Peter ([ClickUp 869egc68v](https://app.clickup.com/t/869egc68v)).

Below: the 08-08 work, still current. Migration **0091** applied staging→prod BEFORE the code.

- **Notes were write-only AND destroyed on edit.** `notes` was missing from the appointments `.select()` on EVERY schedule page, so the calendar handed the dialog `notes: null`, the textarea seeded blank, and saving wrote that blank back. Live prod had 6 appointments and **zero** surviving notes. Fixed on all five surfaces (incl. `/schedule`, the busiest) + the patch now omits `notes` when unchanged, so a future payload loss can't wipe them. `tests/lib/schedule/appointment-notes-contract.test.ts` guards both halves and was **verified to fail** when the bug is reintroduced.
- **Rescheduling a survey told nobody.** Removals have a date-change email; surveys fell through an `appt_type` guard and exited silently. Now sends a "your survey has moved" email + SMS showing the old slot struck through. A resize (start unchanged) sends nothing — it was announcing a move to the same time.
- **Every survey message is now an explicit choice** — a tick box naming the exact address and number, on booking, reschedule and cancel. Calendar drag on a survey routes through the reschedule dialog instead of a bare `confirm()`, because dragging used to fire a real email + billable SMS with nothing on screen saying so.
- **The estimator is told**: new `survey_assigned` push category (0091) + an email carrying address/phone/notes a lock screen must never show. Fires on booked / moved / reassigned-away / cancelled; never notifies you about your own action.
- Adjacent, from the reviews: cancelled surveys now reach the customer; cancelling **or deleting** a money-bearing removal from the diary is refused and points at Bookings (those paths skip the customer email, invoice void, chase pause, `booking_cancelled_at` and refund queue — so a cancelled customer kept being chased); booking reuses the lead's survey row instead of inserting a duplicate that shadowed the first's access photos out of the crew job sheet; completing a survey now stamps the record; explicit "Unassigned" no longer attributes the visit to the booker; the estimator picker no longer lists crew; the quote builder no longer reverts a rescheduled `quotes.moving_date`; a duplicate-suppressed send is reported as suppressed, not sent.
- **NOT fixed — needs Peter's decision** (tracked in ClickUp): a customer who cancels receives nothing in writing about money held; crew are never told a job they're on was cancelled or moved more than ~2 days out (and `appointment_assignments.reminded_at` is written in four places and read in none); there is no no-show concept. (The "`completed` only via Create Quote" item is half-closed: **stats** now derive attendance and are correct; **pay** still keys off the old flag by Peter's instruction.)

## Previous State (2026-08-08 am — audit remediation shipped, prod `2645adf`)

Five-agent audit (lifecycle · comms · cron · UI-truth · money) + read-only live prod sweeps, then nine remediation batches. Prod is on **`2645adf`**; post-deploy health clean each time (all crons ok, 0 failed runs, 0 open operational issues). Migrations went staging→verify→prod→verify, always BEFORE the code needing them.

**PARKED — pick this up first on Monday 10 Aug.** Nine confirmed, deposit-paid jobs (£11,451 of work, £10,501 outstanding) have no diary entry, because nothing auto-created one until 08-07. Until they are in the diary there is no capacity check when quoting, no crew day sheets, and no post-move balance alarm — which is why Rebecca Eldred's £400 sat unnoticed for three days. The backfill is written, dry-run-verified against prod, and sends nothing to anyone. Peter is confirming each row with Luke first. **Review sheet: [`docs/diary-backfill-review.md`](marley-ops/docs/diary-backfill-review.md)** · [ClickUp 869efnx3c](https://app.clickup.com/t/869efnx3c). Re-run the dry run on the day — it derives live. Open question in the sheet: Kristina Butts' date (enquiry 14 Aug vs quote 27 Aug). Peter's call 08-08: do NOT re-send Brydee Thomas's failed confirmation.

**Batches 7-8 (`c3652de`, `8d94b68`, plus `2645adf`).** The tail. `fleet-reminders` failed OPEN twice — a failed settings read was indistinguishable from "feature disabled" and a failed vehicle read from "empty fleet", so the job reported a healthy `{due: 0}` every morning while MOT/tax/insurance reminders stopped (an untaxed van on a job is the business end of that); both reads now throw. Two ledger reads are `strict` — a partial `vehicle_reminder_log` drops exactly the "already sent" rows it exists to remember, and a partial bank-feed open-item set leaves real money unmatched. Refunding a card now closes the double-payment task that asked which payment to refund. "Money paid" on Performance counted deposits + balances but not COMMITMENTS (a real third payment under Policy v2 that /payments already counts), so every confirmed job under-reported by 25%-minus-deposit and the two revenue surfaces could never reconcile. A lost crew day-sheet delivery stamp is surfaced instead of silently re-sending email + billable SMS up to 6× at 19:30.

**Batch 6 (`f80959a`, no migration).** Storage invoices are raised in Zoho first and emailed second, fail-soft — but nothing ever retried the email (the repair sweep only reads `pending` claims), so one Resend blip left customers legally invoiced with nothing in their inbox, permanently, then a second bill next month. New `resendUnemailedStorageInvoices` sweep in the storage-billing cron is that retry layer (this path has no `dispatchComm` behind it). Separately, all three quote-money adopters took an orphan Zoho invoice verbatim: `findInvoiceByReference` returns the total precisely so callers can check ("never adopt a mismatch", and the storage biller does), but adoption also finds invoices Connor raised BY HAND — adopting binds our figure to his PDF and whichever the customer pays leaves one system wrong. Mismatch now releases the claim and raises a money alert; a MISSING total still adopts (absence of evidence isn't a mismatch, and refusing would strand a real orphan).

**Batch 5 (`bee8ab8`, migration 0090).** `commitment_chase_t10_at` gated BOTH the internal "the date isn't confirmed, call them" task and the customer's 25%-due reminder email — whichever fired first consumed the other's one-shot. The internal one fires first by construction, so the real production sequence was: office nudged → customer confirms the date → 25% invoiced → **customer never told**. The T-7 "date at risk" alert then took its grace anchor from the same stamp and told the office "their commitment reminder went out on <date>", which was untrue. New `quotes.date_confirm_nudge_at` separates them (which makes that alert sentence honest by construction), and `confirmMoveDate` now closes the confirm-date task — nothing did, and when 25% ≤ deposit no commitment invoice is raised so `markCommitmentPaid` never ran and the card was permanent. **Behaviour change to review: customers in this state previously got no reminder and now will.** No backfill — a consumed stamp can't be attributed after the fact, and guessing would mean emailing customers whose move already happened.

**Batch 4 (`2177056`, migration 0089 — applied staging→prod BEFORE the code).** Email bounces and complaints are handled at last. A mistyped address was completely invisible: Resend accepts the send with a 200, so the quote email plus all three chases and both deposit reminders were recorded as delivered while every one bounced, and at day 30 the cron marked the lead declined/`no_response` — a false loss for someone who never received anything. The webhook now stamps the send (`communications.bounced_at`/`bounce_kind`), flags the lead (`leads.email_invalid_at`/`email_invalid_reason`), pauses chasing (which also keeps the lead out of the lapse query, killing the false loss), raises one call task naming the bad address, and alerts ops. Classification is its own tested module (`lib/comms/bounce.ts`) defaulting to HARD — reading a soft bounce (full mailbox) as hard would pause chasing on a live job, so only explicit transient markers are treated as soft. `database.types.ts` also picked up 0087's `smtp_fallback_attempted_at`, missing since that migration.

**Batch 3 (`c65e08f`).** A REVISED quote got no follow-ups at all — `dueChaseStep` measures the LEAD's `quote_chase_step` against THIS quote's `email_sent_at` and nothing reset the counter, so a customer already chased 3× had zero chases on the new quote then auto-lapsed to "lost — no response" (sending a quote now resets the ladder, matching how `deposit_chase_step` resets on acceptance). Internal alerts died in exactly the outage they report — the 08-07 SMTP fallback covered `runProviderSend` only, so `sendOpsAlert` and the daily digest both failed silently during a Resend outage; both now use the same guarded fallback. Communications stranded in `queued` (provider accepted, finalise failed) are rescued by a new `sweepStrandedQueuedComms` in the comms-retry cron — that state was swept by nothing, and because finalise stamps `quotes.email_sent_at`, the chase engine skipped those quotes entirely. A settled claim now closes its sign-off-exception follow-up.

**Batch 2 (`26aafcb`) — money traps + two more silent failures.** All were latent not live (verified against prod data) but each turns real money or a real message wrong when it fires: Zoho-sync deposits recorded as `card` made bank-paid money invisible to the refund queue (now `bank_transfer`); an UNCONFIRMED card refund (`needs_review`, reservation intact) read as executed so the queue could close and email the customer "refund issued"; reopening a lost lead left the VOIDED Zoho invoice ids in place so the job could never be invoiced again, and left its refund row executable on a live booking; `createBalanceInvoiceFlow` gained the cancellation guard every sibling money path already had; `markBalancePaid` falls back to the lead's balance so a manually-set balance no longer settles as £0 (suppressed receipt, no VAT); the deposit ops alert now reports the real send outcome instead of always claiming the customer was emailed; `runCommsRetry` reports its query failure instead of returning a clean run while layer 2 of delivery goes dark.

- **Biggest find — the diary gap.** Nothing auto-created the removal appointment, and the ENTIRE post-move settlement engine keys off `appt_type='removal'` ([chase/route.ts](app/api/cron/chase/route.ts)). A booking never added by hand left the system after the deposit: no auto-complete, no review request, no crew sheet, no balance alarm. Live: **9 confirmed deposit-paid jobs (£11,451), zero removal appointments**; Rebecca Eldred moved 5 Aug with £400 uncollected and no alert. FIXED via `lib/schedule/ensure-removal-appointment.ts` (hooked into `markDepositPaid` + `confirmMoveDate`; fail-soft, idempotent, skips iMVE, never invents a date; 6 unit tests).
- **The post-move balance alarm had never fired once.** Its task, ops alert and counter all sat behind one lead-wide `if (!open)` on `reason='balance'` — but raising the final invoice ALWAYS opens such a task. Both halves shipped in `a39bf19`. Now scoped to `source='post_move_overdue'`. **Proven on staging end-to-end**: seeded the exact silencing shape → real cron → `overdueBalances: 1` + stale card superseded.
- Also shipped: chase driving queries error-check + throw (silent zero-chase day was possible); duplicate-guard hit counts as DELIVERED (previously wedged a lead on one step forever); post-move window ordered + 60-day floor (would have saturated, ~20 rows from the iMVE import alone); `inbound_reply`/`no_answer` follow-ups close on accept/deposit; unsigned-contract surfaces exclude iMVE + cancelled (**pre-import blocker**); follow-up cards use the BOOKED date not the enquiry wish date; 25% chip only when a commitment was actually invoiced; /bookings dates in UK time; cancelled bookings dropped from won revenue.
- **Live data is otherwise coherent** — a cross-table contradiction sweep over 8 classes found zero, and every money-layer audit finding is latent rather than active.
- **Open, needs Peter:** (1) backfill the 9 diary-less jobs — `scripts/backfill-removal-appointments.mjs`, dry-run default; past-dated rows email real customers a review request, so they need `--include-past`; (2) Brydee Thomas MMR034's "Move date confirmed" email + invoice never delivered (Resend 2,000-char template-variable limit, since fixed) — needs a manual re-send; (3) card gateway: 5 deposit attempts failed with an identical `response_code=6 / auth 000041` signature on 31 Jul + 3 Aug — raise with takepayments; (4) £2,810 unmatched in the bank feed.
- Findings tracked in ClickUp: [869efjdnj](https://app.clickup.com/t/869efjdnj) diary backfill · [869efjdnp](https://app.clickup.com/t/869efjdnp) failed email · [869efjdnr](https://app.clickup.com/t/869efjdnr) card gateway · [869efjdnx](https://app.clickup.com/t/869efjdnx) money traps · [869efjdnz](https://app.clickup.com/t/869efjdnz) comms · [869efjdp0](https://app.clickup.com/t/869efjdp0) follow-ups · [869efjdp2](https://app.clickup.com/t/869efjdp2) UI truth.

_Prior sessions → brain `O:\brain\01_Projects\Marley Moves\marley-ops CHANGELOG.md` (full "Last touched" history, newest-first; query via `/recall`). This block holds the latest session only — `/ur` evacuates older blocks there. Deployment/ops runbook: `docs/ovh-deployment.md`; go-live checklist: `docs/go-live-checklist.md`._
