@AGENTS.md

## Current State

The premium role-based UI redesign (worktree `O:\projects\red-banana\clients\marley\marley-ops-ui-workflows`, branch `codex/premium-role-ui`) passed browser acceptance at all three role viewports on 12 Jul, and **rollout passes 1 & 2 are complete** — committed locally (`1fe1be4` … `ec9e891`, **NOT pushed**), production `npm run build` green, 278 tests + tsc green. Pass 2 delivered: the redone brand lockup (official full-colour Marley brandmark in a white app-tile + "MARLEY OPS" in **Cormorant Garamond** via next/font, per Peter's feedback), the crew sticky cab action bar, persistent autosave feedback on the quote + cubic builders, calmed colour vocabulary (neutral lead/client action icons), and Bookings aligned to the shared page shell. The only substantive item left is the tab-component unification (a deliberate design-token decision) — see `docs/ui-role-workflows-handoff.md` "Remaining after pass 2". Local dev: Supabase Docker stack + `scripts/seed-dev.mjs` / `seed-dev-crew.mjs` (crew fixtures left in place for review); keep `SANITY_SYNC_DISABLED=true` in `.env.local` (the dashboard otherwise syncs REAL leads from prod Sanity into dev). Do not edit the primary checkout.
